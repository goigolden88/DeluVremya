{
  // Настройки компилятора — семьи, из ядра (Р-83): ядро внутри src/shared
  // собирается по тем же правилам, что у себя. `src` накрывает и подмодуль
  // с его тестами — tsc «Делу Время» проверяет ядро против своих пакетов (Р-84).
  "extends": "./src/shared/tsconfig.base.json",
  "include": ["src", "vite.config.ts"]
}
