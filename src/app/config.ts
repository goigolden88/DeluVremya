/**
 * Конфиг «Делу Время» для ядра `FamilyCore` (Р-83).
 *
 * Всё, чем «Делу Время» отличается для ядра, — одним объектом. Значения
 * перенесены буквально из прежних `core/db.ts`, `core/layout.ts`
 * и `core/importing.ts`: имя базы, хранилища, `V1_STORES`, индексы,
 * раскладка и формат импорта лежат на устройствах и в `DeluVremyaData`,
 * и перевод на ядро их не меняет. Проверка — `config.test.ts`.
 */

import type { AppConfig } from '../shared/core/model.ts'
import { summary } from '../screens/summary.ts'
import { migrations, SCHEMA_VERSION, SYNCED_STORES, type StoreRecord } from './model.ts'

export const config: AppConfig<StoreRecord> = {
  name: 'Делу Время',

  // Своё имя, не 'dnevniki': все приложения семьи живут на одном origin
  // goigolden88.github.io, а IndexedDB общая на origin и различается только
  // именем базы (02-Архитектура, «Локальное хранилище»). Не меняется никогда.
  dbName: 'deluvremya',

  schemaVersion: SCHEMA_VERSION,
  migrations,
  stores: SYNCED_STORES,

  // Раскладка версии 1 — заморожена. Хранилища, появившиеся позже, заводят
  // только их миграции: свежая база строится как версия 1 и доезжает до
  // текущей теми же шагами, что и база установленной копии.
  v1Stores: ['categories', 'presets', 'templates', 'notes', 'time', 'reviews'],

  // Сверх `updatedAt`. Заморожены вместе с `v1Stores` (Я-08 «FamilyCore»):
  // новый индекс — только шагом миграции.
  indexes: {
    categories: [],
    presets: [],
    templates: [],
    notes: ['capturedOn', 'plannedFor'],
    time: ['date'],
    reviews: ['weekStart'],
  },

  // 02-Архитектура, «Раскладка данных в репозитории».
  places: {
    categories: { split: 'none', path: 'categories.json' },
    presets: { split: 'none', path: 'presets.json' },
    templates: { split: 'none', path: 'templates.json' },
    // Месяц — по дате записи, а не по дню в плане: мысль, записанная в марте
    // и поднятая в план в июне, остаётся мартовской. Без даты — undated (Р-08).
    notes: { split: 'month', dir: 'notes', dateOf: (note) => note.capturedOn },
    time: { split: 'month', dir: 'time', dateOf: (block) => block.date },
    reviews: { split: 'none', path: 'reviews.json' },
  },

  storeNotes: {
    categories: 'категории учёта времени',
    presets: 'кнопки быстрого ввода — «Чтение +30»',
    templates: 'шаблоны дня',
    notes: 'заметки: дела, мысли, замыслы и пункты плана — по месяцу, когда записаны',
    time: 'блоки учтённого времени — по месяцу, к которому относятся',
    reviews: 'проведённые обзоры недели',
  },

  importFormat: 'deluvremya-import',

  // Срез итогов для метаприложения семьи — `summary.json` в корне
  // репозитория данных (Р-87; Я-16 «FamilyCore»). Считается из живых
  // синхронизируемых записей и дня, без настроек устройства.
  summary,

  // Свои правила промпта — 1–4; общие ядро допишет следом. Правила про поля
  // живут в описаниях разделов `modules/<имя>/import.ts`, а не здесь.
  promptRules: [
    'Ничего не выдумывай. Чего нет в моих данных — не пиши: необязательное поле опусти, ' +
      'запись без обязательного поля не пиши вовсе, а назови в списке после JSON.',
    'Даты — ГГГГ-ММ-ДД. Любой вид (24.01.26, 20-02-2026, «3 марта») приводи к нему. Где это ' +
      'разрешено и известен только месяц — например, месяц стоит заголовком раздела, — пиши ГГГГ-ММ, ' +
      'без выдуманного числа.',
    // Пределы минут — не здесь, а в описании поля раздела: они живут
    // в модуле, а ядро про модули не знает.
    'Время — в минутах: «1,5 ч» → 90, «1:20» → 80, «40 мин» → 40.',
    'Таблица по дням и занятиям: одна запись на непустую ячейку — день, занятие, минуты. Строки ' +
      'и столбцы итогов, проценты и суммы за месяц не переноси: приложение посчитает их само.',
  ],

  about: {
    data: 'учёт времени, план дня, заметки и обзоры недели',
    privacy: 'внутри заметки, планы и то, куда уходит время',
    sources: 'таблицы учёта времени, заметки или скриншоты из других сервисов',
  },
}
