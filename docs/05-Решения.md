# Решения

Какие были развилки, что выбрали и почему. Отвечает на вопрос «почему так, а не иначе».
Записывается в момент выбора, чтобы через месяц не переобсуждать заново.

---

### Р-01 · Своё приложение, а не готовый планировщик
12.09.2026

Todoist, TickTick, Toggl и десяток похожих закрыли бы половину задачи сегодня и бесплатно.

**Решено.** Своё приложение, второе в семье после «Дневников».

**Почему.** Ни один готовый инструмент не держит план и факт времени в одном месте: планировщики не учитывают время, трекеры времени не планируют. А главная задуманная функция — сказать «ты наметил девять часов дел на шесть свободных» — возможна только там, где лежит и то, и другое. Плюс данные остаются в своём репозитории, а не в чужом облаке с экспортом по подписке.

**Отвергнуто:** связка из двух готовых приложений. Обзор недели тогда собирается руками из двух источников — ровно та работа над инструментом, которая убила таблицу.

**Цена.** Месяцы вместо нуля. Принимается: вторая цель проекта — построить вещь, а ядро уже написано и проверено в «Дневниках» (Р-11).

### Р-02 · Граница с «Дневниками» — по предмету, а не по модальности
12.09.2026

В «Дневниках» (их Р-79) граница названа так: там фиксируется факт, здесь планируется намерение.

**Как было.** По этой формулировке учёт времени — факт, и место ему в «Дневниках». Но там же дневник досуга назначен в тайм-менеджер, то есть исключение появилось раньше самого правила.

**Решено.** Граница проходит по предмету. **«Дневники» — события: тело, вещи, контент, обслуживание. «Делу Время» — время: план и его исполнение.**

**Почему.** План и факт времени обязаны лежать вместе: сравнить «собирался» и «вышло» иначе нельзя, а это главная функция приложения. Разведи их по репозиториям — и обзор недели превращается в кросс-репозиторный джойн ради собственных данных.

**Что не меняется.** Свободных записей в «Дневниках» не будет, как там и решено. Политический дайджест остаётся в Obsidian, кешбеки ждут приложения про деньги — ни то, ни другое не про время.

**Цена.** Формулировка «факт против намерения» из их Р-79 больше не описывает границу точно. Их документ стоит уточнить ссылкой на эту запись.

### Р-03 · Четыре модуля; проекты, иерархия задач и план недели отложены
12.09.2026

Первоначальный список насчитывал семь подсистем: входящие, задачи, проекты, план дня, план недели, обзор, учёт времени, привычки.

**Решено.** В v1 четыре: входящие, план дня, учёт времени, обзор недели. Привычки — повторяющиеся пункты шаблона дня, без своей сущности. Проекты, иерархия задач и отдельный план недели отложены.

**Почему.** Прошлая система умерла от трения, а каждая подсистема добавляет трение на входе. Иерархия проектов — известное место, где персональный планировщик превращается в хобби по его обслуживанию. Привычка от повторяющегося дела отличается ровно ничем, кроме регулярности, и отдельной механики не заслуживает — тот же принцип, по которому в «Дневниках» статус заменил сущность (их Р-21).

**Цена.** Дела лежат плоским списком. Если через три месяца окажется, что плоского не хватает, вернёмся отдельным решением — но с основанием, а не наперёд.

### Р-04 · Мысли — вид записи во входящих, а не модуль
12.09.2026

Заметки и наблюдения велись в Obsidian и заброшены.

**Как было.** Текстовый файл. Запись уходила в пустоту и не возвращалась никогда — та же поломка, что в таблице учёта: платишь каждый раз, не получаешь ничего.

**Решено.** Мысль — запись `kind: 'thought'` во входящих. Механика, ради которой это стоит вести в приложении, одна: **возврат.** В обзоре недели показываются записи месячной и трёхмесячной давности.

**Почему не отдельным модулем.** Свободный текст не получает от приложения ничего: ни сроков, ни итогов, ни напоминаний. Модуль вышел бы слабой копией Obsidian и умер бы по той же причине, что оригинал. Тот же довод, по которому дайджест остался в Obsidian у соседей (их Р-04).

**Отвергнуто и записано на будущее:** отдельный экран «Заметки» со своим поиском — если общий список станет нечитаемым; наблюдение полем самого обзора — надёжнее, но мысль, пришедшая во вторник, деться некуда; мысль комментарием к дню учёта; оставить всё в Obsidian и только напоминать. Сущность во всех вариантах одна и та же, поэтому переход между ними — правка экранов, а не миграция.

**Цена.** У мыслей нет своего места, они лежат вперемешку с делами. На десятках записей незаметно, на сотнях начнёт мешать.

### Р-05 · Полезное и вредное не красит дневной экран
12.09.2026

В таблице учёта были `Sum(Green)` и `Sum(Red)`: категории делились на полезные и вредные, ютуб стоял красным.

**Решено.** Признак у категории остаётся — поле `kind: 'useful' | 'neutral' | 'idle'`. **Но на дневном экране он не красит ничего.** Работает только в обзоре недели.

**Почему.** Ежедневный красный на 34% ютуба даёт вину, вина даёт избегание приложения, а избегание — конец учёта. В обзоре тот же признак осмыслен: там смотрят на неделю целиком, а не оправдываются за вечер.

**Цена.** Часть наглядности таблицы теряется. Если окажется, что окраска мотивировала, а не давила, — вернуть её вопрос одной строки стилей.

### Р-06 · Название и адрес — «Делу Время», `DeluVremya`
12.09.2026

Имя становится адресом навсегда: `base`, `start_url`, `scope`, `id` манифеста и пути иконок выводятся из него.

**Решено.** Приложение — «Делу Время», репозиторий — `DeluVremya`, адрес — `goigolden88.github.io/DeluVremya/`.

**Почему.** «Делу время, потехе час» — приложение ровно про обе половины: дела и досуг. Название само объясняет границы продукта. Регистр тот же, что у `Dnevniki`: простое русское существительное латиницей, семья читается с одного взгляда.

**Отвергнуто:** `delu-vremya` (привычнее для npm, но ломает пару с `Dnevniki`), `Vremya` (теряет половину смысла), `Uklad`, `Ritm`, `Tempo`.

**Цена.** Переименование репозитория ломает адрес; `base`, `start_url`, `scope` и `navigateFallback` правятся вместе с ним.

### Р-07 · Приоритетов нет: приоритет — это попадание в план и одно главное дело
12.09.2026

Стандартный ход — поле приоритета P1–P4 у задачи.

**Решено.** Поля приоритета нет. Вместо него: **что попало в план на сегодня — то и приоритет**, плюс **ровно один слот «главное дело дня»**, в который второе не помещается. У входящих вместо приоритета — возраст: запись, висящая дольше порога, попадает в обзор с вопросом «в план / когда-нибудь / удалить».

**Почему.** За месяц в любой системе всё становится P1, и поле перестаёт значить что-либо. Приоритет к тому же переоценивается каждый день, а поле хранит вчерашнюю оценку. Акт планирования и есть расстановка приоритетов — второй механизм для того же лишний.

**Цена.** Нельзя отсортировать входящие по важности. Сортировка по возрасту предлагается вместо этого, и её достаточно: висящее месяц важно само по себе.

### Р-08 · Импорт — свой строгий формат; дата необязательна
12.09.2026

Три источника чужих записей: февральская таблица, дневник досуга и заметки из Obsidian.

**Решено.** Парсер чужих форматов не пишется. Приложение принимает только свой строгий JSON `deluvremya-import`; чужое к нему приводит ИИ по промпту, который собирается из тех же описаний полей, по которым идёт проверка. Импорт только добавляет: совпавшее по естественному ключу пропускается.

**Дата записи необязательна.** Заметка без даты принимается, уезжает в `notes/undated.json`, видна в списке и в поиске — но **в возврат наблюдений не попадает**: показывать «месяц назад» нечего.

**Отвергнуто:** ставить недатированной записи дату импорта. Двести старых заметок объявили бы себя сегодняшними и забили бы возврат мусором — то есть сломали бы единственную механику, ради которой мысли здесь и ведутся (Р-04).

**Цена.** `capturedOn` в модели нуллабельна, и это держит весь код: нарезка по годам, лента, возврат. Плата за честность даты.

### Р-09 · `share_target` и `shortcuts` — основной путь ввода
12.09.2026

Быстрые мысли сейчас уходят в «Избранное» Телеграма, и работает это по одной причине: Телеграм уже открыт и стоит в системном меню «Поделиться».

**Решено.** В манифесте объявляются `share_target` (приложение появляется в системном «Поделиться» рядом с Телеграмом; ссылка или текст из любого приложения падает во входящие) и `shortcuts` (долгий тап по иконке даёт «Записать», «План дня», «Учесть время»).

**Почему это архитектурное решение, а не украшение.** Приложение конкурирует не с Obsidian, а с уже открытым мессенджером. Если захват дороже, чем в Телеграме, оно проиграет независимо от того, насколько хороши остальные экраны. Обработка share идёт в service worker, а значит закладывается в Этап 0, а не прикручивается потом.

**Цена.** Работает только на Android у установленного приложения. Во вкладке и на компьютере ввод обычный — приемлемо: быстрый захват нужен с телефона.

### Р-10 · Четыре продуктовых модуля, два кодовых; обзор недели — экран
12.09.2026

Отступление от привычной раскладки «модуль на модуль», объявленное до кода.

**Решено.** Кодовых модулей два: `notes` (входящие и план дня) и `time` (учёт времени). Обзор недели модулем не является и лежит в `screens/Review.tsx`.

**Почему.** Входящие и план дня работают с одной сущностью (Р-12) — разносить их по модулям значит завести два модуля, знающих друг о друге, то есть нарушить главное правило границ в первый же день. Обзор по природе читает всё сразу; в `screens/` это законно — экраны и `app.tsx` единственные, кому позволено знать все модули.

**Цена.** Слова «модуль» в документах значат разное в зависимости от того, о чём речь — о продукте или о коде. Названо явно здесь и в Архитектуре, чтобы не путалось.

### Р-11 · Ядро копируется из «Дневников», общее не выносится
12.09.2026

`db`, `sync`, `github`, `layout`, `id`, `dates`, `sw`, прогон в браузере и скрипт иконок в «Дневниках» написаны, проверены и работают на двух устройствах.

**Решено.** Копировать в новый репозиторий как есть. Общий пакет не заводится.

**Почему.** Абстракция, выведенная из двух случаев, выводится по одному из них и ломается на третьем. Правило трёх: приложений про еду и про деньги ещё нет, и как они отличаются — неизвестно.

**Требование к копированию:** копировать **буквально**, без правок «заодно улучшу». Тогда через год diff покажет, что действительно разошлось, и вынос общего станет механическим, а не археологией.

**Отвергнуто:** пакет из GitHub с версиями — обслуживание версий на ровном месте при одном авторе.

**Цена.** Ошибка, найденная в «Дневниках», чинится дважды. При одном авторе и двух репозиториях это дешевле, чем преждевременный общий пакет.

### Р-12 · Одна сущность `Note` на входящее, дело, наблюдение и пункт плана
12.09.2026

Четыре продуктовых понятия, которые напрашиваются на четыре типа записей.

**Решено.** Одна структура `Note` с полями `kind` ('task' | 'thought'), `plannedFor` (дата или null), `status`, `main`, `estMin`. Входящее — `plannedFor: null`; пункт плана — дата проставлена; наблюдение — `kind: 'thought'`.

**Почему.** Запись живёт одной жизнью: мысль «купить фильтр» рождается во входящих, попадает в план на среду, выполняется. При четырёх типах нужна механика превращения одного в другой, и она протечёт в ленту, в поиск, в синхронизацию и в импорт. При одной сущности превращение — правка двух полей. Тот же приём, что в «Дневниках»: статус вместо сущности (их Р-21), поле вместо сущности (их Р-30).

**Цена и признак, что решение не выжило.** У пункта плана начнут появляться поля, не нужные входящему никогда. Записано в слабые места Архитектуры, чтобы через месяц это не выглядело внезапным. Пересмотр — новым решением, а не тихой правкой модели.

### Р-13 · Вид записи по умолчанию при захвате — дело
13.09.2026

Повод: минимальный захват из «Поделиться» в Этапе 0 (Р-09). Модель требует `kind`, а при захвате полей нет ни одного.

**Решено.** Запись, у которой вид при захвате не выбран, получает `kind: 'task'`. Переключается одним тапом — при записи или позже, в том числе через месяц.

**Почему.** Ошибки несимметричны. Дело, записанное мыслью, не попадёт ни в план дня, ни под вопрос «в план / когда-нибудь / удалить» (Р-07) и сгниёт молча — ровно поломка «Избранного» Телеграма. Мысль, записанная делом, всплывёт в разборе и переключится одним тапом.

**Отвергнуто:** `'thought'` по умолчанию — по причине выше; спрашивать вид при захвате — лишнее действие против Р-09; третье значение «не разобрано» — правка модели ради одного случая, когда разбор и так ритуал.

**Цена.** Мысли без выбранного вида попадают в разбор дел и тратят там тап на переключение.

### Р-14 · Напоминания — вместе с тем, о чём напоминать: о дне в Этапе 1, об обзоре в Этапе 5
13.09.2026

Повод: напоминания есть в ядре `01-Проект` и в Архитектуре (`notify.ts`, ключи `reminder*`), но ни в одном этапе Плана.

**Решено.** Механика — как в «Дневниках» (Р-11): периодическая фоновая проверка, окно со звуком, журнал пробуждений. Напоминание о незаполненном дне — последний пункт Этапа 1, об обзоре — пункт Этапа 5. Что считать незаполненным днём и как звучит вопрос — решается в этих этапах, по ходу.

**Почему.** Напоминать о незаполненном дне не о чем, пока нет учёта; об обзоре — пока нет обзора. Учёт времени — та самая привычка, которая умерла в прошлый раз, поэтому напоминание нужно ему с первых дней, а не отдельным этапом в конце. Отдельный этап «напоминания» спроектировал бы механику под догадки.

**Цена.** Этап 1 растёт на пункт. Приход напоминания проверяется днями — браузер будит проверку сам, примерно раз в сутки, — поэтому проверка на телефоне идёт хвостом и закрытие этапа не держит.

### Р-15 · Ядро копируется целиком в Этапе 0, синхронизация подключается в Этапе 2
13.09.2026

Повод: план работ Этапа 0. Уточняет порядок Плана; Р-11 не меняет.

**Как было.** По Плану `github.ts` и `layout.ts` появлялись в Этапе 2, в Этапе 0 — только `db`, `id`, `dates`.

**Решено.** В Этапе 0 копируется всё ядро «Дневников» с одного их коммита: `db`, `id`, `dates`, `github`, `layout`, `sync` — вместе с тестами. Сначала отдельным коммитом байт в байт, следующим — правки по делу этого проекта. Синхронизация к экранам не подключается: ни автозапуска, ни настроек. Этап 2 — подключение, настройки и проверка живым запросом.

**Почему.** Один снимок с одного коммита, и правки отдельным диффом — расхождение с «Дневниками» читается целиком, как требует Р-11. Тесты раскладки с первого дня проверяют нуллабельную `capturedOn` и `notes/undated.json` — решение, которое держит весь код (Р-08).

**Цена.** Около тысячи строк лежат неподключёнными до Этапа 2. Исправление, найденное за это время в «Дневниках», переносится сюда руками.

### Р-16 · Приём из «Поделиться» — через адрес, без service worker; картинки отложены
13.09.2026

Уточняет Р-09: там обработка share отдана service worker.

**Как было.** По Р-09 и Архитектуре service worker принимает запрос `share_target` и открывает приложение с подставленным текстом. Приём картинки обещан наравне с текстом.

**Решено.** `share_target` с `method: 'GET'`, `action` — корень приложения, параметры `title`, `text`, `url`. Android открывает `/DeluVremya/?text=…`; приложение при старте, до первого экрана, одной чистой функцией переводит адрес во внутренний `#/inbox?shared=…` и чистит строку адреса, чтобы перезагрузка не приняла текст второй раз. Ярлыки — тем же входом: «Записать» → `?go=inbox`, «Учесть время» → `?go=time`, «План дня» — корень. Service worker в приёме не участвует. Принимаются текст и ссылки; картинки — в «Отложено».

**Почему.** Такую навигацию уже обслуживает работник — отдаёт index.html из кеша, без сети, — а без работника её отдаёт сам GitHub Pages: страница открывается при любом состоянии кеша. Своя обработка в работнике нужна только для `POST`, а `POST` — только ради файлов. У `POST` плохой отказ: если работник в этот момент не управляет страницей (после очистки данных, при сбое обновления), запрос уходит на GitHub Pages, тот отвечает 405, и расшаренное теряется молча. Параметр запроса, в отличие от `#` в адресе ярлыка, доезжает до страницы наверняка. Оба приёма целиком проверяет прогон в браузере, открывая `?text=…` и `?go=…`; телефону остаётся проверить одно — что Android передаёт параметры.

**Отвергнуто:** `POST` с обработкой в работнике — ради картинок, которым нет места в модели; работник, перенаправляющий `GET` на адрес с `#`, — второй путь к тому же, и приложению всё равно нужен свой на случай, когда работник не управляет страницей.

**Цена.** Картинку во входящие не кинуть. Понадобится — `POST`, обработка в работнике, поле в модели и хранение файлов в репозитории данных: новое решение.

### Р-17 · Чужие стили «Дневников» удаляются разом, первым коммитом Этапа 1
13.09.2026

Повод: `styles.css` скопирован из «Дневников» целиком, и около двух третей в нём — оформление их экранов: циклы, траты, графики, контент. Ядром по Р-11 он не считается, буквальность копии на него не распространяется.

**Решено.** Одним коммитом в начале Этапа 1 удалить всё, чего не использует ни один экран этого проекта. Понадобится похожее — берётся из «Дневников» или из истории git.

**Почему.** Уборка по мере своих экранов растягивается на месяцы, и часть чужого остаётся навсегда. Разом — одна сессия, и прогон подтверждает, что ничего не отвалилось. Не в Этапе 0 — чтобы проверка автообновления шла на неизменённом каркасе.

**Отвергнуто:** удалять по мере своих экранов — по причине выше.

**Цена.** Стили, которые пригодились бы позже (графики — обзору недели), вернутся руками.

### Р-18 · Идущий таймер — настройка устройства, а не запись
13.09.2026

Повод: открытый вопрос «Где живёт идущий таймер», план работ Этапа 1. В `TimeBlock` нет времени начала.

**Решено.** Идущий таймер лежит в `settings` под ключом `timer`: `{ categoryId, bgCategoryId?, startedAt }`. Остановка превращает его в обычный `TimeBlock` — дата по началу (Р-19), минуты по разнице, перед записью их можно поправить. Модель не меняется.

**Почему.** Запись без минут — это блок, который ломает всё, что с блоками работает: суммы дня, естественный ключ импорта, правило слияния, выгрузку. Ради неё в модель пришлось бы добавить `startedAt` и научить каждый расчёт пропускать незакрытые блоки. Таймер — состояние одного устройства: запускают и останавливают его на одном и том же телефоне.

**Отвергнуто:** блок с `startedAt` и без минут — по причине выше; таймер в памяти страницы — теряется при закрытии приложения, а на телефоне его закрывает сама система.

**Цена.** Таймер, запущенный на телефоне, на компьютере не виден и после синхронизации (Этап 2). Остановить его можно только там, где запустили.

### Р-19 · Граница дня — календарная полночь; блок таймера — днём начала
13.09.2026

Повод: открытый вопрос «Граница дня». Блок, записанный в час ночи, ложится в новый день.

**Решено.** «Логической полуночи» нет. Тап по пресету ставит блок в календарное «сегодня». Блок таймера получает дату начала: ютуб с 23:30 до 00:30 — вчерашний. У поля даты в ретро-вводе — кнопки «сегодня» и «вчера».

**Почему.** Настраиваемая граница («день кончается в 4 утра») протекает в каждое «сегодня» приложения — итог дня, план, напоминание, обзор недели — и в каждое место, где дата сравнивается с календарём. Случай, который она лечит, закрывается одной кнопкой «вчера».

**Отвергнуто:** граница дня настройкой — по причине выше; делить блок таймера через полночь на два — два блока там, где человек помнит одно дело.

**Цена.** Тап по пресету после полуночи за вчерашний вечер ляжет в сегодня; поправить — «вчера» в правке блока.

### Р-20 · Тап по пресету добавляет блок; отмена — кнопкой, а не повторным тапом
13.09.2026

Уточняет План, Этап 1, п. 2: там «повторный тап снимает».

**Решено.** Каждый тап по «Чтение +30» ставит новый блок: два тапа — шестьдесят минут. Последний тап отменяется кнопкой «Отменить» в строке отклика. Любой блок дня правится и удаляется из списка дня.

**Почему.** Кнопка с плюсом обещает прибавку. Повторный тап-снятие делает час чтения пресетами недостижимым, а случайный двойной тап превращает запись в ничто — молча. Отмена кнопкой видна и не случается сама.

**Отвергнуто:** повторный тап снимает — по причине выше; тап прибавляет минуты к уже стоящему блоку той же категории — правка чужого блока вместо своего, и «отменить последнее» перестаёт быть удалением одной записи.

**Цена.** Отмена позже, чем через строку отклика, — два действия: найти блок в списке дня и удалить.

### Р-21 · «Неучтено» считается от окна дня, а не от суток
13.09.2026

Повод: План, Этап 1, п. 5 — «сколько осталось неучтённым». Неясно, от чего.

**Решено.** У дня есть окно — константа кода, с 8 до 24. Неучтённое — прошедшая часть окна минус учтённое; у прошедших дней — всё окно. Показывается с основанием, «неучтено 3 ч 10 мин из прошедших 9 ч», серым, не красным.

**Почему.** От суток — это требование закрыть 24 часа, от которого учёт и бросают (`01-Проект`). От всего окна утром — «неучтено 14 часов» в десять утра, то есть ложь. Прошедшая часть окна — единственное, про что вопрос «куда ушло» имеет смысл.

**Отвергнуто:** не показывать неучтённое вовсе — пропадает отклик, ради которого пункт стоит в Плане; окно настройкой в интерфейсе — рано, пока не видно, что константа мешает.

**Цена.** Кто живёт не с 8 до 24, видит неверное основание, пока окно не станет настройкой.

### Р-22 · Категорию можно удалить; с блоками — только переносом в другую
13.09.2026

Повод: просьба человека — категории добавлять, удалять, переименовывать, «как в Дневниках». На экране категорий удаления не было, только архив; решение об этом жило в комментарии кода, не в документах.

**Решено.** Пустая категория удаляется после подтверждения — надгробием, вместе со своими кнопками. Категория с блоками удаляется только переносом: блоки — и где она основная, и где фоновая — переходят в выбранную категорию, затем надгробие. Если после переноса фоновая совпала бы с основной, фоновая снимается. Архив остаётся — для категории, которой больше не размечают, но чьи блоки должны остаться под своим именем. Переименование в занятое название не сливает категории: слияние — это удаление с переносом.

**Почему.** Удалённая категория с блоками — полуживое состояние: имя блоки помнят по надгробию, а в выборе и в правке её нет. Перенос закрывает и удаление, и слияние одним механизмом. Так же устроено в «Дневниках» (их `removePlan`).

**Отвергнуто:** удалять вместе с блоками — учтённое время пропадает молча; сливать при переименовании в занятое название, как в «Дневниках», — второй путь к тому же, и опечатка в названии сливала бы категории.

**Цена.** Перенос переписывает каждый блок категории: после года учёта — сотни правок одним действием и одним коммитом синхронизации.

### Р-23 · Реестр видов записей растёт по этапам: сейчас — подпись и импорт
13.09.2026

Отступление от Архитектуры, «Реестр видов записей»: там у строки подпись, лента, markdown и раздел импорта. Объявлено до кода, человек согласился.

**Решено.** `src/registry.ts` заводится в Этапе 1 со строкой на каждый вид записи: подпись и, где есть, раздел импорта. Строки ленты и markdown добавляются в Этапе 6 вместе с `core/feed.ts`. `core/importing.ts` и экран импорта берутся с коммита «Дневников» `a913dcb` — байт в байт отдельным коммитом, правки следующим (Р-11).

**Почему.** Лента и markdown тянут `core/feed.ts` и экран ленты — это Этап 6; заводить их ради импорта значит проектировать наперёд. Полноту таблицы по видам компилятор проверяет и сейчас.

**Цена.** В Этапе 6 — правка каждой строки реестра.

### Р-24 · Незаполненный день — за сегодня ни одного блока к пробуждению проверки
13.09.2026

Повод: План, Этап 1, п. 7; Р-14 оставил определение этапу. Человек: механика — ровно как в «Дневниках».

**Решено.** Механика «Дневников»: периодическая фоновая проверка, звук только в окне (по умолчанию 12–20), вне окна — тихое, со звуком не чаще раза в день, журнал пробуждений, «Проверить сейчас». Напоминает, если за сегодня не записано ни одного блока; тап ведёт на «Время». Имя фоновой проверки — `remind`, общее на все напоминания приложения: обзор недели (Этап 5) встанет под него же.

**Почему.** Сегодня, а не вчера: главная поломка таблицы — ретроспективный ввод, и напоминание о вчерашнем толкало бы именно к нему. Ни одного блока, а не «мало»: порог «мало» — оценка, и она давит (Р-05). Имя общее, потому что после выпуска его не сменить — переименование молча выключает проверку у всех; в «Дневниках» имя первого напоминания так и осталось на все остальные.

**Отвергнуто:** вчерашний день пуст — по причине выше; порог по минутам.

**Цена.** Время пробуждения выбирает браузер. Разбудил в полдень — напомнит и в день, когда к вечеру всё записалось бы и так.

### Р-25 · Прошлый день на «Времени» — в адресе `?day=`, кнопки пишут в показанный день
13.09.2026

Повод: блок на вчера записывается, но увидеть и поправить его потом негде. Человек отдал решение разработке. Уточняет Р-20: там тап ставит блок сегодняшним днём.

**Решено.** У даты на «Времени» — «‹» и «›», в будущее не листается, к сегодняшнему — одной ссылкой. День лежит в адресе, `#/time?day=ГГГГ-ММ-ДД`. Кнопки, итог, список блоков и «задним числом» работают с показанным днём; над кнопками прошлого дня — «записывается на …». Таймер — только на сегодняшнем: он всегда про сейчас. Главный экран «Сегодня» — всегда сегодня.

**Почему.** Прошлый день — тот же экран с другой датой, а не новая сущность: весь расчёт уже принимает дату. В адресе — чтобы день переживал перезагрузку и кнопку «назад», а ярлык `?go=time` по-прежнему открывал сегодня.

**Отвергнуто:** ждать ленты Этапа 6 — полтора этапа без правки вчерашнего; календарь выбора дня — дольше двух стрелок, а дальше недели назад листают редко, для этого есть поле даты в «задним числом».

**Цена.** Тап на экране вчерашнего дня пишет во вчера. Подпись над кнопками обязательна, иначе ошибка незаметна.

### Р-26 · Названия вкладок — настройка устройства; по умолчанию «Сегодня», «Учёт», «Входящие»
13.09.2026

Повод: проверка на телефоне. Человек: название «Время» вводит в заблуждение; названия экранов должны переименовываться в «Настройках». Я предлагал одно постоянное имя — человек выбрал настройку: тексты, называющие экран, собираются подстановкой.

**Решено.** Экран учёта по умолчанию называется «Учёт»; адрес `/time`, ярлык «Учесть время» и имена в базе не меняются. Названия трёх вкладок — «Сегодня», «Учёт», «Входящие» — меняются в «Настройках» и лежат в `settings` под ключом `screenNames`. Всё, что называет экран, — вкладки, заголовки, приветствие, подсказки, текст уведомления — берёт имя из одного места, `ui/screenNames.ts`. В тексте имя стоит в именительном падеже и в кавычках — «экран «Учёт»», а не «на «Учёте»»: вписанное человеком имя не склоняется. Тест-сторож падает на названии вкладки в кавычках, написанном в исходнике руками.

**Почему.** Название — дело вкуса, а цена настройки при одном источнике — одна функция и тест. Возражение «тексты разойдутся с именем» снимается тем же приёмом, что числа в справке: текст собирается из значения, а не пишется руками.

**Отвергнуто:** только новое постоянное имя — человек хочет своё; синхронизировать названия — нужно новое хранилище, то есть миграция, ради настройки вкуса; склонять свои имена — без словаря не решается, а именительный в кавычках читается нормально.

**Цена.** Названия у каждого устройства свои: переименованное на телефоне на компьютере остаётся прежним. Ярлыки по долгому тапу и название приложения зашиты в манифест при установке и за настройкой не следуют. Строки «Что нового» — история и хранят старые имена.

### Р-27 · День на экране учёта выбирается и календарём, а не только стрелками
13.09.2026

Уточняет Р-25: там календарь отвергнут — «дальше недели назад листают редко».

Повод: импорт февраля. История ушла на месяцы назад, и до неё — две сотни тапов «‹». Человек: «офигею листать».

**Решено.** Между «‹» и «›» — поле даты браузера, `<input type="date">`: на Android по тапу открывается системный календарь. Будущий день не выбирается. День по-прежнему лежит в адресе `?day=`, стрелки остаются для соседних дней.

**Почему.** Предпосылка Р-25 не пережила первого же импорта. Системное поле — ноль зависимостей и родной календарь телефона; свой календарь — компонент ради одной задачи.

**Отвергнуто:** свой календарь с отметкой дней, где есть записи, — полезно, но это уже обзор истории, лента Этапа 6; кнопка «к последнему дню с записями» — прыжка к конкретной дате не даёт.

**Цена.** Вид поля зависит от браузера: на компьютере — выпадающий календарь Chrome, на iPhone — колесо.

### Р-28 · Записи режутся по месяцам, а не по годам
13.09.2026

Закрывает открытый вопрос «Нарезка блоков времени — по году или по месяцу». Отступление от Архитектуры, «Раскладка данных в репозитории»: там `notes/2026.json` и `time/2026.json`. Объявлено до кода, человек согласился.

**Как было.** Раскладка «Дневников» — файл на год. Проход синхронизации отправляет изменившийся файл целиком, второе устройство целиком его скачивает.

**Числа.** Настоящий февраль в каноническом виде файла раскладки: 173 блока, 33 КБ, 195 байт на блок. В этом темпе год — около 2250 блоков и 440 КБ. К декабрю каждое нажатие кнопки отправляло бы 440 КБ, а второе устройство скачивало бы около 590 КБ: GitHub отдаёт содержимое в base64. Файл месяца — 35 и 47 КБ, и так весь год. Прежняя оценка «около мегабайта» завышена вдвое, вывод тот же.

**Решено.** Блок времени лежит в `time/ГГГГ-ММ.json` по `date`, заметка — в `notes/ГГГГ-ММ.json` по `capturedOn`. Запись без разбираемой даты — как прежде, в `<хранилище>/undated.json` (Р-08). Справочники и обзоры — одним файлом, как были.

**Почему и заметки.** Их объёма ещё нет, но план дня и привычки шаблонов порождают заметку на каждый день — порядок тот же, что у блоков. Первая синхронизация заведёт файлы заметок, и после неё смена раскладки — переезд файлов. Одно правило на оба хранилища проще двух.

**Почему сейчас.** Синхронизация не подключена ни в одной выпущенной сборке (Р-15): файлов старой раскладки нет нигде, смена ничего не стоит. Правка давнего дня переписывает только его месяц.

**Отвергнуто:** годы, как в «Дневниках», — по числам выше; месяц только для блоков — второе правило ради записей, объём которых неизвестен.

**Цена.** Два десятка файлов в год вместо двух. Ещё одно расхождение с ядром «Дневников» — вариант нарезки в `core/layout.ts`.

### Р-29 · Одноимённые категории сливаются сами; надгробие помнит, куда слито
13.09.2026

Повод: план работ Этапа 2. Человек: коллизию одинаковых названий решить архитектурно, приоритет — у более поздней записи. Правка модели, объявлена до кода, человек принял.

**Как было.** Id категории выводится из названия, `cat:<название>`, — и у заведённой руками, и у заведённой импортом. Одно название на двух устройствах — одна запись, и при встрече побеждает поздняя правка по общему правилу. Дубль остаётся возможен, когда оба устройства правят до обмена: на одном «Шахматы» переименовали в «Игры» — id остался `cat:шахматы`, — на другом завели «Игры», `cat:игры`. Или две разные категории переименовали в одно название.

**Решено.** После прихода данных с сервера или из файла-копии живые категории с одинаковым названием — регистр и пробелы по краям не различаются — сливаются в одну:

- **остаётся** та, чей id совпадает с названием, `cat:<название>`; такой нет — с наименьшим id;
- **содержимое** — название, признак, порядок, архив — берётся у версии с самым поздним `updatedAt`;
- блоки остальных, основные и фоновые, переходят в оставшуюся, их кнопки — тоже; кнопка с минутами, которые у оставшейся уже есть, убирается;
- остальные уходят надгробием с новым полем **`movedTo`** — id той, куда слиты.

Блок или кнопка, чья категория — надгробие с `movedTo`, переходит туда, куда оно указывает, по цепочке. Удаление с переносом (Р-22) тоже пишет `movedTo`.

**Почему остаётся не поздняя.** В момент слияния устройства могут видеть разное время правки: одно правило у себя и ещё не отправило. Телефон оставил бы одну, компьютер другую, и после обмена надгробиями оказались бы обе, а блоки разъехались бы в разные стороны. Правило по id на всех устройствах даёт один ответ. Поздняя правка решает, что в записи написано, — в этом её приоритет.

**Почему `movedTo`.** Блок, записанный на втором устройстве в категорию раньше, чем туда доехало её слияние или удаление, приезжает к надгробию. Без поля он остаётся под именем удалённой — то полуживое состояние, от которого уводила Р-22. До синхронизации такой гонки не было: оба действия шли на одном устройстве.

**Отвергнуто:** оставлять позднюю — по причине выше; сливать руками удалением с переносом — дубль заметят не сразу, и блоки разойдутся по двум «Играм»; выводить id из названия и при переименовании, заводя новую запись, — переименование переписывало бы все блоки категории и ключи кнопок, а гонка с надгробием осталась бы; искать, куда делся блок, по названию надгробия — после переименования имя не совпадает.

**Цена.** Поле в модели — правка Архитектуры. Версия схемы не меняется: индекса нет, прежние записи и файлы читаются как есть. Слияние идёт без участия человека и переписывает блоки второй категории. Пустая категория удаляется без выбора, куда переносить, — блок, приехавший к ней позже, остаётся под её именем, как прежде.

### Р-30 · Карточка записи во входящих: вид, текст, «Сделано», «Удалить»
13.09.2026

Повод: план работ Этапа 3. План называл только смену вида позже. Расширение объявлено до кода, человек согласился.

**Решено.** Тап по записи открывает её карточку на месте: переключатель «Дело | Мысль», правка текста, «Сделано» — только у дела: `status: 'done'` и `doneOn`, — и «Удалить» с подтверждением, надгробием. После «Сделано» — строка отклика «Убрано из входящих · Отменить».

**Почему.** Без «Сделано» выполненное дело до Этапа 4 можно только удалить: факт теряется, и приучается неверный жест. Без удаления нечем убрать двойной «Поделиться» или опечатку. Модель не меняется: всё это — поля, которые уже есть.

**Отвергнуто:** только смена вида, как в Плане, — по причинам выше.

**Цена.** Сделанное не видно нигде до плана дня (Этап 4) и ленты (Этап 6). Отклик об этом говорит — пропадает не молча.

### Р-31 · Замысел — третий вид записи; дело относится к нему ссылкой, иерархии нет
13.09.2026

Повод: план работ Этапа 3. Человек попросил «Планы» третьей группой — глобальные замыслы вроде «съездить на Байкал», «открыть бизнес», «выучить английский», а не набор конкретных дел, — и связь с делами, которые к ним относятся. Там же — свои группы записей: добавлять, удалять, переименовывать. Правка модели, объявлена до кода.

**Решено.**

- `NoteKind` получает третье значение `'goal'`, на экране — «Замысел». Выбирается при записи и в карточке, как дело и мысль. По умолчанию по-прежнему дело (Р-13)
- Замысел не лежит в неразобранном: у него свой сворачиваемый блок «Замыслы» на экране заметок. Возраст у него виден, но в разбор висяков (Этап 5) он не попадает — висеть долго ему положено
- Дело относится к замыслу ссылкой в `refs`: строка `note:<id замысла>`. Одно дело — один замысел, выбирается в карточке дела. У замысла видно, сколько к нему дел и сколько из них сделано. «Сделано» у замысла называется «Достигнут» — тот же `status: 'done'`
- Слово «Планы» не берётся: «план» в приложении — план дня (`plannedFor`), и два смысла одного слова путали бы

**Почему так мало.** Одна ступень — замысел и его дела — отвечает на просьбу и не заводит иерархии. Подзадачи у дел, этапы замысла и процент выполнения — это проекты, отложенные Р-03: место, где планировщик становится хобби. Модель почти не растёт: новое значение перечисления и поле `refs`, заведённое под связи между записями с первого дня. Версия схемы прежняя: хранилищ и индексов не прибавилось, прежние записи читаются как есть — как с `movedTo` (Р-29). Вид записи в начале ссылки оставляет место связям с другими видами и приложениями семьи.

**Отвергнуто:**

- «Планы» статусом `someday` — «когда-нибудь» говорит о сроке, а не о размере: за замысел можно браться уже сейчас
- свои группы записей со своими названиями — новый справочник, первая миграция, и каждой группе нужно поведение «как дело» или «как мысль», иначе приложение не знает, что делать с её записью в плане и в обзоре. Три вида закрывают названное; вернуться, если за месяц их окажется мало
- отдельное поле `goalId` — второе место для связи, когда под связи уже есть `refs`

**Цена.** Связь с удалённым замыслом или с записью, переделанной в другой вид, просто не показывается. Устройство на старой сборке покажет замысел без подписи вида, пока не обновится само. Обзор недели, вероятно, захочет «замыслы без движения» — решается в Этапе 5.

### Р-32 · Экран входящих по умолчанию называется «Заметки»
13.09.2026

Уточняет Р-26: там умолчания «Сегодня», «Учёт», «Входящие».

Повод: план работ Этапа 3. Человек предложил «Мысленное»; я возразил, что оно прячет дела — вид по умолчанию (Р-13), — и предложил «Заметки». Человек согласился.

**Решено.** Вкладка и заголовок экрана по умолчанию — «Заметки». Адрес `/inbox`, ключ `inbox` в `screenNames`, ярлык «Записать» и имена в коде не меняются. То, что ещё не разобрано, внутри экрана называется «Неразобранное»; «входящие» остаются в документах названием продуктового модуля.

**Почему.** Экран вмещает все три вида записи (Р-31), а «заметка» в обиходе — и мысль, и дело. Своё имя по-прежнему ставится в «Настройках».

**Цена.** Устройство со своим именем вкладки его и сохраняет; у остальных вкладка сменится с обновлением. Строки «Что нового» хранят старое имя.

---

## Открытые вопросы

- **Порядок Этапов 1 и 3.** Учёт времени идёт раньше входящих, потому что февральская таблица даёт настоящие данные для проверки расчётов в первый же день. Если слезть с «Избранного» Телеграма окажется срочнее — меняются местами, это правка Плана, не Архитектуры.
- **Путь интеграции с «Дневниками».** Один репозиторий данных с папками, токен на чтение чужого или файл вручную. Решается не раньше Этапа 5: до него обзору нечего читать.
- **Напоминание в точное время.** В v1 окно 12–20 без звука вне его. Если окажется мало — календарь либо обёртка в APK; пуш через GitHub Actions отвергается за ненадёжность расписания.
- **Нужен ли отдельный план недели** сверх плана дня и обзора. Выяснится за первый месяц.
- **Фоновое время в итогах категорий.** Блок «Ютуб, фоном Игры» в сумму дня входит один раз. В итогах недели по категориям фоновое время, вероятно, должно попасть и в «Игры» — тогда сумма категорий превысит учтённое время суток, как было в итоговой аналитике февральской таблицы. Решается в Этапе 5 вместе с «план против факта».
- **Сводка за период** — месяц по категориям — отложена человеком до обзора недели. Суммы февраля с таблицей сверяются по дням.

Поднятые человеком в плане работ Этапа 3, 13.09.2026. Решаются до кода этапа:

- ~~Название экрана входящих~~ — закрыто Р-32; ~~«Планы» третьей группой~~ и ~~свои группы записей~~ — закрыты Р-31
- **Приоритет** — человек назвал его возможным на будущее. Прямо противоречит Р-07. Возврат — только новым решением с объяснением, почему Р-07 не выдержало
- **Срок и напоминание о нём.** Поле срока — необязательное поле `Note`, без индекса миграции не требует. Напоминание — только ежедневной фоновой проверкой (Р-14, Р-24): «срок сегодня» или «завтра», в час, который выберет браузер. К точному часу — без сервера нельзя (План, «Отложено»). Место по смыслу — Этап 4, план дня
- **Прогресс пункта плана.** Статусы в модели есть: `open`, `done`, `someday`, `dropped`. Процент выполнения у дела на один день — шум; у дела на много дней — это проект (Р-03). Решается в Этапе 4 вместе с закрытием дня