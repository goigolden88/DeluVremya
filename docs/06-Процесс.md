# Процесс

Обновлён: 14.09.2026

Как ведётся работа над проектом. Не про то, что строим, а про то, как.

## Где что живёт

| Что | Где |
|---|---|
| Код приложения | `github.com/goigolden88/DeluVremya`, публичный; сайт — `goigolden88.github.io/DeluVremya/` |
| Документы проекта | `docs/` в том же репозитории |
| Инструкции для Claude Code | `CLAUDE.md` в корне репозитория |
| Данные пользователя | IndexedDB на устройстве; с Этапа 2 — отдельный приватный репозиторий данных |
| Эталон ядра | «Дневники», `github.com/goigolden88/Dnevniki` и локальная копия — **только для чтения** |

Документы лежат вместе с кодом: `CLAUDE.md` подхватывается автоматически, а документы версионируются заодно с кодом. Копия в проекте claude.ai — снимок; правится только репозиторий.

### Сессии Claude Code

Запускать из корня репозитория: история бесед привязана к рабочей папке. Между этапами — `/clear`, `/compact` — для середины этапа. Перед `/clear`: дерево чистое, всё запушено, и по одному Журналу понятно, где мы и что дальше.

### «Дневники» — только читать

Из их репозитория ничего не правится и не коммитится, даже найденная ошибка: о ней говорится человеку. Код оттуда берётся с конкретного коммита через `git archive` — байт в байт, не по памяти и не из рабочей папки. Сначала отдельным коммитом как есть, следующим — правки по делу этого проекта: так дифф второго коммита и есть всё расхождение (Р-11, Р-15).

## Разделение работы

**Claude Code — и «что строить», и «как».** Продуктовые развилки и пересмотры архитектуры — тоже здесь: он видит настоящие файлы и проверяет факты. Режим планирования — когда обсуждаем и ничего не трогаем.

**Чат (Claude Desktop, claude.ai) — когда компьютера нет под рукой**, для свободного разговора без кода и для картинок. Его итог приходит в репозиторий потоком ниже.

### Планирование в Claude Code

1. Сессия планирования из корня репозитория — заготовка в «Шаблонах»
2. Решения — в `05-Решения` в момент выбора; правки `01`, `02`, `03` — по ходу
3. Конец: Журнал, передача, коммит документов, дерево чистое
4. `/clear` — и код новой сессией по плану. Контекст планирования в неё не переезжает: память — документы

## Поток изменений из чата: чат → документы → код

1. Обсуждаем крупное изменение в чате
2. На выходе — готовые куски markdown для вставки: запись в `05-Решения`, правки `01-Проект` и `02-Архитектура`
3. Отдельным пунктом — что придётся переделать в уже написанном коде
4. Вставляем, коммитим
5. Следующая сессия Claude Code начинается со сверки

## Обратный поток: код → чат

То, что Claude Code в конце сессии называет «не описано в документах», возвращается в обсуждение, а не решается на месте.

## Правила ведения документов

- **Не менять задним числом.** Отмена решения — новая запись со ссылкой
- **Решения записываются в момент выбора**
- **Журнал обновляется в конце каждой сессии**
- **Перед пушем — `npm test`, `npm run build`, `npm run smoke`**, итог по коду выхода, а не по выводу
- **Прогон проверяется порчей кода**, когда меняется его сценарий: сломанная строка экрана обязана дать код 1
- **Видимое изменение — строка в `src/changes.ts`** в том же коммите
- **План содержит только предстоящее**

## Шаблоны

Начало сессии по плану:

```
Работаем по docs/03-План.md, следующий этап оттуда.
Прочитай CLAUDE.md и docs/ перед началом.
Сначала покажи план работ, код пиши после подтверждения.
```

Сессия планирования:

```
Планируем: <что — новый этап, модуль, развилка>. Прочитай CLAUDE.md
и docs/. Код не пишем — обсуждаем по одной развилке, с твоей
рекомендацией, и записываем решения в docs/ в момент выбора.
В конце — Журнал, передача в 06-Процесс, коммит документов.
```

Начало сессии после обновления документов в чате:

```
Я обновил docs/. Прочитай их и сверься с кодом.
Скажи, что теперь расходится и в каком порядке это править.
```

Конец сессии:

```
Дай текст для docs/04-Журнал.md: что сделано, в каком состоянии,
следующий шаг. И для docs/05-Решения.md, если по ходу что-то решили.
Отдельно — что осталось не описанным в документах.
```

### Передача в следующую сессию

Переписывается в конце каждого этапа. Сейчас передаёт состояние Этапа 6 — код написан 14.09.2026, ждёт проверки на телефоне.

```
Проект «Делу Время». Работать в корне репозитория.

Прочитай `CLAUDE.md` и все файлы `docs/` перед началом.

Этап 6 «Лента, выгрузка, справка» написан и запушен, ждёт проверки
на телефоне — см. последние записи `docs/04-Журнал.md` и План, Этап 6,
«Что проверить на телефоне». Сайт — goigolden88.github.io/DeluVremya/,
деплой — GitHub Actions на пуш в main; `gh` на компьютере нет — деплой
проверяется по скрипту живого сайта. Тесты — vitest, 687; прогон —
`npm run smoke` (CDP, без Playwright), 130 проверок, идёт дольше десяти
минут — только в фоне; синхронизация в нём — на подставном GitHub; схема
данных — 1. Компьютер и телефон синхронизируются через приватный
репозиторий данных.

Следующее — по ответу человека о проверке закрыть Этап 6 (хвосты —
в План), затем Этап 7 «Отдать другу» по `docs/03-План.md`. Начни с плана
работ, код после подтверждения. Отложенное человеком до конца всех
этапов — раздел Плана «После Этапа 7 — доведение до ума»; его не трогать.

Ключевое, что нужно знать до кода:
— лента — экран `#/feed` (Р-62); строки и раздел markdown — у каждого
  вида в реестре, `feed` и `markdown` обязательны (Р-63); учёт — строкой
  на день (Р-58), заметка — днём записи, лента только читает, переход
  `#/inbox?open=<id>` — у неразобранного и замысла (Р-59); обзор — днём
  проведения, строки в `screens/reviewFeed.ts` (Р-60);
— `core/feed.ts` — из «Дневников» с правками: без даты — внизу, дата
  ищется и словами; «Заметки» ищут тем же правилом ядра (Р-61);
— «Что нового» — `useWhatsNew` на «Сегодня», ключ `seenChanges` (Р-65);
  видимая правка — строка в `changes.ts` в том же коммите; Этап 6 — запись 17;
— справка `#/help` — числа только из констант, названия экранов —
  из настроек; сторож цифр `help.test.ts` — на справку и приветствие (Р-64);
— журнал ошибок — `ui/report.ts`, ключ `errorLog`; у ошибки — путь
  экрана без запроса: в запросе бывает текст из «Поделиться» (Р-66);
— прогон стирает `seenChanges` прямо в IndexedDB и шлёт ошибку страницы
  событием `ErrorEvent`: настоящее исключение валит прогон само;
— итоги месяца и года — экраны `#/month?m=ГГГГ-ММ` и `#/year?y=ГГГГ`
  в `screens/`, без записи о проведении (Р-54); какой месяц и год
  показать и какой месяц закрыла неделя — `screens/period.ts`; вход —
  карточка «… закончился» в обзоре недели и ссылка внизу «Сегодня»;
  неделя — месяцу своего воскресенья, `weeksEndingIn` в `core/dates` (Р-55);
— история нормы — `norm.since`: полные недели с понедельника не раньше
  него, строка «выполнена в N из M» — при трёх неделях в счёт; одно
  правило `historyOf` на неделю, месяц и год (Р-53, Р-56);
— график — `ui/bars.ts` (геометрия, тесты) и `ui/BarChart.tsx` (SVG);
  цвет `--chart` проверен скриптом палитры: тёмный акцент для столбцов
  слишком светел (Р-57); перед новым графиком — тот же скрипт;
— тест настоящего февраля читает `seed/` через `import.meta.glob`,
  без файла пропускается; типов Node в проекте нет;
— после `git mv` старый путь в `git add` роняет add целиком — после
  коммита смотреть `git show --stat`;
— порча для прогона — скриптом в папке сессии, замена ложится ровно раз;
  наложил — сперва `tsc`, потом сборка и прогон в фоне: сломанная
  сборка тратит десять минут впустую;
— обзор недели — экран `#/review?week=` в `screens/` (Р-10, Р-41); неделя
  с понедельника — `weekStart`, `weekPeriod` в `core/dates`; к обзору —
  в воскресенье текущая, иначе прошлая, долг не копится; один обзор на
  неделю — id `review:<понедельник>` (Р-42); `screens/review.ts` знает
  только неделю, запись обзора, когда звать и пороги;
— расчёты обзора — в модулях и принимают промежуток дней `Period`, а не
  неделю (Р-52): итоги и нормы — `modules/time/period.ts`, план против
  факта — `modules/notes/period.ts`, висяки, «когда-нибудь», замыслы без
  движения, возврат — `modules/notes/review.ts`; на них стоят месяц и год;
— фоновое время в сумму категории не входит, у категории — отдельной
  строкой (Р-43); план против факта — по `plannedFor`, `doneOn` — день
  факта, удалённые не считаются (Р-44);
— норма недели — поле `Category.norm` { minDays, minMinutes, maxMinutes },
  «раз» — день с блоком; вместо серии — последние четыре недели; на
  «Учёте» — свёрнутый блок «Неделя», только «не меньше» (Р-45);
— висяки — по пять за обзор, выбираются при открытии экрана; `someday` —
  блок «Когда-нибудь» на «Заметках» (Р-46); наблюдение недели — обычная
  мысль, `Review.refs` ссылается на неё, `Review.note` не используется (Р-49);
— пороги обзора — ключи устройства `reviewStaleDays`, `reviewGoalDays`,
  по умолчанию 30 и 28 (Р-48); напоминание об обзоре — та же проверка
  `remind`, вс и пн, ключи `reminderReviewDay`, `reminderReviewQuietDay` (Р-51);
— прогон зависит от дня недели: в вс и пн обзор зовёт, сценарий смотрит
  на `reviewCallDay()`; своё нужной давности сценарий обзора заводит импортом;
— коммиты — через Bash с heredoc: here-string PowerShell 5.1 уходит
  в `git commit` аргументом, а не в stdin;
— план дня живёт на «Сегодня» (Р-33), логика — `modules/notes/plan.ts`,
  шаблоны — `modules/notes/templates.ts`, экран `#/templates`; пункт плана —
  `Note` с `plannedFor`; хвост прошлых дней ждёт решения, вечернего
  ритуала нет (Р-34); реализм — оценки против остатка окна дня 8–24,
  остаток передаёт экран, модули друг о друге не знают (Р-35); главное —
  одно, при двух — позднее по `updatedAt` (Р-40); шаблон ставит пункты без
  дублей по тексту (Р-39); приоритета, срока и прогресса нет (Р-36…Р-38);
— в сценарии прогона клик — последнее действие шага: после клика React
  перерисовывает в микрозадаче, второй клик того же шага видит старое;
— base `/DeluVremya/`; манифест с ним сверяет прогон по собранному, а не исходник;
— база `deluvremya`: один origin с «Дневниками», имя базы — единственное, что их разводит;
— раскладка версии 1 заморожена: хранилища categories, presets, templates,
  notes, time, reviews; новое хранилище или индекс — только миграцией;
— ядро скопировано из «Дневников» с коммита a913dcb; номера Р-NN
  в комментариях ядра — их, не наши; «Дневники» — только читать;
  копия оттуда — байт в байт отдельным коммитом, правки следующим;
— синхронизация подключена: любая запись в базу уезжает сама, экраны
  перечитывают данные по `db.onChange` — и приехавшие с другого устройства;
  заметки лежат в `notes/ГГГГ-ММ.json` по `capturedOn`, без даты —
  в `notes/undated.json` (Р-28, Р-08);
— `Note` — одна сущность на входящее, дело, мысль, замысел и пункт плана
  (Р-12, Р-31): входящее — `plannedFor: null`; вид по умолчанию — дело (Р-13);
  замысел — `kind: 'goal'`, своим блоком; дело относится к замыслу строкой
  `note:<id>` в `refs`, одна ступень, без иерархии (Р-03, Р-31);
  `capturedOn` и `plannedFor` — разные вещи; дата необязательна (Р-08);
  порядок записей — по `capturedOn`, а не по `id`;
— экран входящих по умолчанию называется «Заметки» (Р-32), адрес — `/inbox`;
— адреса ярлыков и share зашиты в установленное приложение: `?go=inbox`
  и `?go=time` обязаны работать всегда (Р-16); `?go=inbox` ведёт
  на `/inbox?write=1` — курсор в поле; прошлый день — `#/time?day=`;
— имена в базе устройств не меняются никогда: ключи настроек `timer`
  (Р-18), `reminder*`, `sync*`, фоновая проверка `remind` (Р-24);
— id категории — из названия; одноимённые с двух устройств сливаются сами,
  надгробие хранит `movedTo` (Р-29); удаление с блоками — только переносом (Р-22);
— реестр `src/registry.ts` полный: подпись, лента, markdown и, где есть,
  импорт (Р-23, Р-63); формат импорта — `deluvremya-import`, разделы
  `notes` и `time`;
— названия вкладок — только из `ui/screenNames.ts` (Р-26): в тексте —
  именительный в кавычках, тест-сторож падает на имени, вписанном руками;
  адреса экранов от названий не зависят;
— формы с числами — `noValidate`: пределы и причину называет свой код;
— прогон меняется вместе с экраном и проверяется порчей кода: сломанная
  строка обязана дать код 1; вывод проверок — в папку сессии: `$TMPDIR`
  в этой оболочке пуст;
— личные данные и копии — не в репозиторий кода (он публичный), под `.gitignore`.

Как работаем: сначала план работ, код после подтверждения. Решения — в `docs/05-Решения.md` в момент выбора. Отступления от Архитектуры — вслух до кода. В конце сессии — Журнал. Перед `/clear` дерево чистое и всё запушено.
```
