# Журнал

Что сделано и в каком оно состоянии. Отвечает на вопрос «где мы сейчас».
Обновляется в конце каждой беседы. Новые записи сверху.

---

## 13.09.2026 — Этап 1: учёт времени написан целиком, ждёт проверки на телефоне

**Решения — все записаны в момент выбора**

- **Р-18 — идущий таймер — ключ `timer` в настройках устройства.** Блок без минут сломал бы суммы, импорт и слияние; цена — таймер телефона не виден на компьютере
- **Р-19 — граница дня — календарная полночь; блок таймера — днём начала.** «Логическая полночь» протекла бы в каждое «сегодня»; случай закрывает кнопка «вчера»
- **Р-20 — тап по кнопке добавляет блок, отмена — кнопкой.** Отменяет «повторный тап снимает» из Плана: с ним час чтения кнопками недостижим, а двойной тап молча обнулял запись
- **Р-21 — «неучтено» — от прошедшей части окна дня 8–24**, а не от суток
- **Р-22 — категория удаляется: пустая — сразу, с блоками — только переносом.** Перенос заодно и слияние. По просьбе человека, как в «Дневниках»
- **Р-23 — реестр растёт по этапам: сейчас подпись и импорт.** Отступление от Архитектуры, объявлено до кода; лента и markdown — Этап 6
- **Р-24 — незаполненный день — за сегодня ни одного блока.** Имя фоновой проверки `remind`, общее на все напоминания
- **Р-25 — прошлый день на «Времени» — в адресе `?day=`, кнопки пишут в показанный день.** Человек отдал решение разработке

**Сделано**

- Первым коммитом — уборка чужих стилей «Дневников» (Р-17); прогон прошёл без правок сценария
- Дата «Сегодня» сменяется в полночь без перезапуска — хук `ui/useToday`, таймер к полуночи и пересчёт при возврате из фона
- Категории и кнопки: стартовый набор с id из названия и неподвижным штампом, заводится слиянием при пустом хранилище — два устройства дублей не заведут. Порядок, переименование, признак (нигде не красит, Р-05), архив, удаление с переносом. Экран `/time/categories`
- Вкладка «Время», экран `/time` — цель ярлыка `?go=time`. Кнопки категорий и на «Сегодня»; итог дня сразу под ними — сумма с числом блоков, разбивка, фоновая отдельно, неучтённое с основанием
- Таймер и «задним числом» — одна форма на ввод и правку блока, минуты по медиане своей истории категории; «Фоном» — в форме и в таймере
- Листание дней «‹ ›» на «Времени»
- Импорт: `core/importing.ts` и экран импорта с `a913dcb` байт в байт отдельным коммитом (`7f5dcf2`), правки — следующим; `src/registry.ts`, раздел `time`, промпт из тех же описаний, сводка до записи. «Настройки» → «Экспорт и импорт» → «Импорт записей»
- Напоминание о незаполненном дне: `src/notify.ts` по механике «Дневников», обработчик `periodicsync` в `sw.ts`, раздел «Напоминания» в «Настройках»
- «Что нового» — записи 3 и 4, по строке на видимое изменение

**Находки по ходу**

- Нативная проверка формы: у поля минут стоит `min`, и браузер сам не пускал ноль своей подсказкой на своём языке — наша причина с пределами не показывалась. Формы с числами — `noValidate`. Поймал прогон
- Слово «таймер» есть в подписи «задним числом», а проверка прогона не различает регистр: «таймера нет» проверялось бы ложно. Проверка — по заголовку блока, а не по тексту
- Реестр «Дневников» связан с лентой и markdown через `core/feed.ts` — копировать его ради импорта значило тащить Этап 6 → Р-23
- Естественный ключ импорта склеивает два одинаковых блока одного дня. Для таблицы с суммами по ячейкам это верно; граница записана в Архитектуру
- `window.confirm` в безголовом браузере некому нажать — в сценарии подменяется
- Удаление категории у «Дневников» — не отдельная функция в модуле циклов, а связка экрана и плана записи; повторена механика, не код

**Ошибки по ходу, мои**

- В тесте сдвига категорий ждал изменённые записи в обратном порядке. Порядок выдачи — не требование; поправлен тест, код верен
- Вставив в прогон блок категорий, не учёл, что шаг настроек начинался с «Сегодня», где живёт шестерёнка, — восемь проверок упали не по делу. Поправлено переходом; ловится тем, что прогон запускается на каждой порции
- `min` у числового поля без `noValidate` — недосмотр; поймал сценарий, написанный на это же правило

**Состояние: код этапа написан, этап не закрыт**

*Проверено машинно*

- `tsc` — 0; `vitest` — 336 тестов в 23 файлах, все прошли; сборка — 0; прогон — 58 проверок, ошибок нет; в собранном `sw.js` нет `import`/`export`
- Каждый новый кусок сценария проверен порчей кода, прогон падал с кодом 1 ровно на своих проверках: сдвиг категорий, сумма дня, медиана, фоновая в сумме, перенос фоновой при удалении, правило незаполненного дня, разбор дня из адреса, пропуск повторов при импорте
- Копия импорта из «Дневников» — хеши блобов совпадают с `a913dcb`

*Проверено на устройстве*

- Ничего нового. Шаги 0–5 выкатывались по ходу, на телефоне их не смотрели

*Не проверено*

- Всё «Готово когда» Этапа 1 — пункты проверки в Плане, Этап 1
- Импорт настоящего февраля — человек проверяет вручную; машина проверена на тестовых данных
- Приход напоминания самого по себе — хвост Этапа 1 в Плане

**Не описано в документах — на обсуждение**

- **Сводка за месяц по категориям.** Условие этапа — «суммы февраля сходятся с таблицей», а смотреть их можно только по дням. Либо сводка периода на «Времени» сейчас, либо сверка тестом на локальной копии данных (как `realdata.test.ts` в «Дневниках»), либо ждать обзора недели
- **Заметка блока** (`note`) импортируется и хранится, но на экране не видна и не правится
- **Архивную категорию удалить нельзя**, не вернув её из архива сначала
- **Удаление с переносом необратимо** иначе как руками: блоки переписаны. Подтверждения у переноса нет — есть явный выбор категории и отдельная кнопка
- **Нарезка блоков по году или месяцу** — по-прежнему до Этапа 2, на числах настоящего февраля после импорта

**Следующий шаг**

Проверка Этапа 1 на телефоне по пунктам Плана и ручной импорт февраля. Этап 1 закрывается ответом человека; затем — Этап 2, до первого файла в репозитории данных решить нарезку блоков.

---

## 13.09.2026 — Этап 0: каркас закрыт — ставится, работает без сети, обновляется сам

**Решения — все записаны в момент выбора**

- **Р-13 — вид записи по умолчанию — дело.** Дело, записанное мыслью, сгниёт молча; мысль, записанная делом, всплывёт в разборе и переключится тапом
- **Р-14 — напоминания вместе с тем, о чём напоминать.** О незаполненном дне — последний пункт Этапа 1, об обзоре — Этап 5; механика «Дневников»
- **Р-15 — ядро целиком в Этапе 0.** `db`, `id`, `dates`, `github`, `layout`, `sync` с тестами; синхронизация подключается в Этапе 2
- **Р-16 — «Поделиться» через адрес, без service worker; картинки отложены.** `share_target` — GET на корень, разбор в `src/launch.ts`; ярлыки — `?go=`
- **Р-17 — чужие стили «Дневников» удаляются разом**, первым коммитом Этапа 1

**Сделано**

- Документы из планирования закоммичены как есть, до кода. Заведены `04-Журнал` и `06-Процесс` — на них ссылался `CLAUDE.md`, а файлов не было
- Ядро «Дневников» с их коммита `a913dcb` — байт в байт через `git archive`, отдельным коммитом `b21ecac`. Следующий, `5b5dd76`, — все правки по делу проекта: модель дословно из Архитектуры, база `deluvremya`, хранилища и индексы, раскладка, тексты, `sw.ts` без напоминаний, манифест с `share_target` и тремя ярлыками, иконка-часы
- Экраны: «Сегодня» с приветствием первого запуска; «Настройки» — «О приложении» и «Копия всех данных»; «Входящие» — минимальный захват и список
- `src/launch.ts` — адрес запуска в маршрут до первого экрана; из его же таблицы `vite.config.ts` собирает ярлыки и параметры share
- Прогон: обвязка из «Дневников», сценарий Этапа 0
- `src/changes.ts` — список «Что нового» с первого дня, окно — Этап 6
- Акцент интерфейса тёплый, в тон иконке: два приложения на одном телефоне не путаются

**Находки по ходу**

- Оба приложения на одном origin `goigolden88.github.io`. IndexedDB различается только именем — буквальная копия `DB_NAME = 'dnevniki'` открыла бы базу «Дневников». Кеши работника разведены сами: `cleanupOutdatedCaches` удаляет только кеши своего scope — проверено по коду workbox
- Приём «Поделиться» в service worker означал бы `POST`, а `POST` при неуправляющем работнике уходит на GitHub Pages, получает 405 и теряет расшаренное молча → Р-16
- Картинки через «Поделиться» обещаны в `01` и Архитектуре, а поля для них в модели нет → отложены
- Номера Р-NN в комментариях скопированного ядра — номера «Дневников». Без пометки «Р-07» в `db.ts` читалось бы как наше «приоритетов нет»; записано в Архитектуре
- Грубая оценка: годовой файл блоков времени к декабрю — около мегабайта, и синхронизация гоняла бы его целиком → открытый вопрос о нарезке по месяцам, решить до Этапа 2
- Доедет ли `#` в адресе ярлыка WebAPK — не уверен; параметр запроса доезжает наверняка, поэтому ярлыки на `?go=`
- «Поделиться» у YouTube на компьютере приложения не показывает: это собственное окно сайта YouTube, а приём share у PWA есть только на Android (Р-09). Ожидаемо, не поломка
- В системном меню «Поделиться» на телефоне приложение пока в самом конце списка; Android поднимает его по мере использования, на части телефонов его можно закрепить долгим тапом

**Ошибки по ходу, мои**

- Две правки подключения входящих сделал наполовину: импорт экрана без маршрута и таблицу ярлыков в `vite.config.ts` без импорта. Заметил, перечитав свои правки, до проверки типов; `tsc` поймал бы и так
- В сообщение коммита `974ff98` вместе с числом тестов попал хвост вывода: поиск строки «Tests» без учёта регистра зацепил и «tests 29%». Косметика; число с тех пор беру одной строкой, итог — по коду выхода

**Состояние: этап закрыт**

*Проверено машинно*

- `tsc` — 0; `vitest` — 232 теста в 13 файлах, все прошли; сборка — 0; в собранном `sw.js` нет `import`/`export`
- Прогон — 26 проверок, ошибок нет: манифест под `/DeluVremya/`, иконки отдаются, приветствие, запись во входящие, «Поделиться» с подстановкой и чисткой адреса, ярлыки, настройки, копия файлом туда и обратно, service worker, без сети — и приложение, и «Поделиться»
- Порча строки экрана входящих — прогон упал с кодом 1, восемь проверок не сошлись
- Деплой: GitHub Actions, запуск 34748785296 на `e483869` — «Сборка» (с тестами) и «Публикация» успешны; источник Pages уже стоял на GitHub Actions. На `goigolden88.github.io/DeluVremya/` страница, её скрипт и стили, иконки, манифест и `sw.js` отвечают 200; ни одной ссылки страницы мимо `/DeluVremya/`; в опубликованном манифесте `id`, `start_url`, `scope` — `/DeluVremya/`, share — GET, ярлыков три
- Сборка для проверки автообновления — запуск 34749153048 на `59b1980`, успешен; живой сайт отдаёт сборку 13.09.2026 14:14:13

*Проверено на устройстве*

- Android, со слов: «Проверил, всё робит — дата сборки 13.09.2026 14:03:31» — установка иконкой и проверка по списку: без сети, «Поделиться», ярлыки
- Автообновление, со слов: «авто обновление прошло — время поменялось на то, что ты указал» — сборка 14:03:31 сменилась на 14:14:13 сама, без переустановки
- «Поделиться» с телефона: «появилась, только надо мотать в самый конец» — приложение есть в системном меню, пока в хвосте списка

*Не проверено*

- Приём из разных приложений по отдельности — Chrome, YouTube, Телеграм кладут текст в разные параметры. Записано в хвосты Этапа 0 в Плане, проверяется в Этапе 3
- iPhone — не проверялся вовсе. Хвосты Этапа 0

**Не описано в документах — на обсуждение**

- Когда считается «первый релиз», после которого `V1_STORES` заморожен. Я считаю им установку на телефон — она случилась 13.09.2026, значит раскладка версии 1 уже заморожена
- Дата в шапке «Сегодня» не пересчитывается, если приложение открыто через полночь, — известная грабля «Дневников». Становится важной в Этапе 1, где блоки времени ставятся «сегодняшним днём»; записано в передачу

**Следующий шаг**

Этап 1 «Учёт времени» по `03-План`, новой сессией после `/clear` — промпт в `06-Процесс`, «Передача в следующую сессию». Первый коммит этапа — уборка чужих стилей (Р-17). К сессии — февральская таблица учёта досуга: её импорт — пункт 6 этапа, и на её числах решается нарезка блоков времени.
