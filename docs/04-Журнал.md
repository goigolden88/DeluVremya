# Журнал

Что сделано и в каком оно состоянии. Отвечает на вопрос «где мы сейчас».
Обновляется в конце каждой беседы. Новые записи сверху.

---

## 24.09.2026 — ядро до Я-24: `meta.json` называет приложение

Сопровождение по промпту ядра (Журнал `FamilyCore`, «итоги первой сессии „Тотального Учёта“»). Прочитаны Я-23…Я-26 «FamilyCore» и та запись.

**Сделано**

- `eb0f3c4` — указатель ядра `b0047b6` → `6a5da3f`. Приехало: Я-21 — комментарии среза; Я-24 — `meta.json` `{ app, schemaVersion }`, `app` — `deluvremya`; чужой `app` — проход стоит, ничего не скачав и не отправив; давний без `app` принимается, имя дописывается; Я-26 — `share` — доля. Видимого нет — строки в `changes.ts` нет
- `02-Архитектура` — `meta.json` в раскладке, шаг 4 прохода, «Версионирование схемы» — ссылкой на договор ядра; токен «семья» (Я-25) с оговоркой о переходе; строка рисков о токене
- `03-План` — хвосты «Ядро с Я-24»: синхронизироваться хотя бы раз; токен «семья» и справка — после всех четырёх
- `06-Процесс` — `gh` на компьютере есть (человек подключил в другой беседе): строка в «Где что живёт», шаблон передачи — `gh run list` и `gh issue list` вместо `curl` к api.github.com

**Состояние: ядро с Я-24 в сборке; `app` в `DeluVremyaData` — за человеком**

*Проверено машинно* (итоги по коду выхода)

- `npm test` — 0, 32 файла, 479 тестов; `tsc --noEmit` — 0; `npm run build` — 0; `npm run smoke` — 0, 146 проверок. Скрипта `npm run typecheck`, названного в промпте ядра, здесь нет — типы проверяет `tsc` в `build` (Р-84)
- `gh`: вход `goigolden88` fine-grained токеном; `gh run list` и `gh issue list -R goigolden88/DeluVremya` читают — деплои видны, issues ноль

*Не проверено*

- Настоящий проход с давним `meta.json` — `app` появится при первой синхронизации после деплоя. Ping-pong `meta.json` со старой сборкой на втором устройстве (Я-24 «FamilyCore», «Цена», п. 2) — ожидаем не больше одного лишнего коммита
- Права `gh`-токена на запись — не проверялись и не нужны были

**Не описано в документах — на обсуждение**

- Справка «Синхронизация между устройствами» (`screens/Help.tsx`) говорит «токен к нему» — после перехода на «семью» верна не до конца. Поправить при переходе, строкой в `changes.ts` (План)
- Шаблон передачи `06-Процесс` держит числа перевода на ядро — 449 тестов и 144 проверки; сейчас 479 и 146. Шаблон переписывается в конце этапа — не трогал
- `03-План`, таблица находок вычитки разведки: срез итогов стоит «следующим», хотя сделан (Р-87, запись ниже)
- Промпт ядра называет `npm run typecheck` у всех приложений; здесь его нет — для ядра, если промпты общие

**Следующий шаг**

Человек: открыть приложение с сетью, синхронизировать хотя бы раз, проверить в `DeluVremyaData/meta.json` `"app": "deluvremya"`. Затем — по записи ниже: итог сессии ядра о срезе, метаприложение

---

## 24.09.2026 — Срез итогов для метаприложения (Р-87)

По промпту ядра после Я-16…Я-20 «FamilyCore». План работ с тремя развилками; человек: «По развилкам согласен, с остальным тож».

**Решения — записаны в момент выбора**

- **Р-87** — ключи и подписи среза; нормы идущей недели — `open`; «обзор не проведён» — в день зова приложения, только за кончившуюся неделю; отрезок без блоков — `no-data`

**Сделано**

- `61fdd44` — указатель ядра на `b0047b6` (срез итогов в договоре)
- `c0cae69` — `groupTotals` в `modules/time/groups.ts`: у группы минуты, блоки, дни (объединение, а не сумма) и фон; `periodSummary` и `daySummary` несут `byGroup`; `Period`, `TimeDay`, `YearTime` берут суммы оттуда. Сумма у группы кнопок — теперь вся группа за день: строка «Что нового»
- `bafde20` — `screens/summary.ts` и поле `summary` в `app/config.ts`. `PLAN_FACT_BASIS` переехал в `modules/notes/period.ts`: `notes/labels.ts` тянет `db` через `ui/screenNames.ts`, и конфиг замкнулся бы в цикл
- Документы: `02-Архитектура`, «Срез итогов» — таблица ключей; `summary.json` в раскладке и дереве кода; `06-Процесс` — строка памятки

**Состояние: срез пишется, машинно проверен; настоящий репозиторий — за человеком**

*Проверено машинно* (итоги по коду выхода)

- `tsc --noEmit` — 0; `npm test` — 0, 32 файла, 478 тестов. В `summary.test.ts` каждый случай — через `checkSummary(buildSummary(...))` ядра, плюс настоящий февраль из `seed/`: прошлый месяц на 2 марта — 211 ч 50 мин, учёт в 28 днях из 28
- `npm run build` — 0; `npm run smoke` — 0, 146 проверок: `summary.json` приезжает тем же коммитом, на сегодня, с четырьмя отрезками; ни одного из семи текстов заметок в нём нет; повтор без правок — ни одного коммита, то есть срез детерминирован
- **Порча — в отдельной рабочей копии** (`git worktree`, первый раз по правилу `06`): переименованный ключ и текст заметки в подписи — код 1, не сошлось ровно 2 новые проверки из 146. Правило рабочей копии проверено: копия и переход сняты, основная папка порчи не видела

*Проверено на настоящих данных*

- Человек синхронизировал телефон и прислал `summary.json` из `DeluVremyaData` (посчитан 24.09.2026). `parseSummary` ядра принял его — разовым скриптом в папке сессии, Node без сборки. Минуты групп вместе с «Без группы» равны `time.total` во всех отрезках: 5670, 2820, 8790; август — `no-data`; внимание пусто (четверг); ключи — пять групп человека. Строк `norm.…` нет — норм у человека нет, со слов
- Нашлись два огреха оснований: «из 1 сделанных» при единице и оценки голыми минутами — «из 870 мин». Поправлены: склонение после «из», `formatMinutes`; тест на единицу

*Не проверено*

- Прогон на копии настоящих данных — копии нет; вместо неё — настоящий февраль в тесте и присланный срез

**Ошибка по ходу, моя**

- Перед порчей `git add -A && git stash` убрал незакоммиченный срез в stash — хотел завести рабочую копию, а не прятать работу. Сразу вернул `git stash pop`, потерь нет; дальше — коммит, потом рабочая копия от `HEAD`

**Не описано в документах — на обсуждение**

- **Для ядра:** прошлый отрезок с `through: null` назван окончательным, хотя ретро-ввод его правит — форма знает только `lastEdit`. Нормы месяца историей «выполнена в N из M недель» у приложения есть, в составе договора — нет; не добавлено
- **Для ядра:** README давних репозиториев данных о `summary.json` не скажет (Я-16 «FamilyCore», «Цена», п. 4) — у нас README уже лежит
- Прежние — в записях ниже

**Следующий шаг**

Человек передаёт итог сессии ядра: срез принят `parseSummary`, ключи — для таблицы «Состав», что не сошлось с договором. Дальше — по ядру: метаприложение.

---

## 24.09.2026 — Вычитка разведки ядра; путь интеграции — метаприложение (Р-86)

Кода нет — только документы.

**Что было.** Сессия ядра разведала, что приложения семьи могут отдать метаприложению (`FamilyCore`, `docs/07-Разведка.md`). Сводку «Делу Время» вычитала отсюда эта сессия на `ed95582`: только чтение, итог — промптом в беседу ядра. Человек согласился со всем. Поправки ушли в таблицы разведки, новое — в её раздел «Вычитка», «Делу Время». Главное из поправок:

- тема `day` от устройства не зависит — только её текст; зависит идущий таймер (Р-18) и пороги обзора (Р-48);
- у норм нет третьего исхода «не ясно»; модель не различает «не учитывал» и «не делал» — минуты без основания `days` из `elapsedDays` врут;
- суммы по группам склеивает экран; у месяца два смысла — время по дням, нормы по неделям с воскресеньем (Р-55).

**Что решило ядро** (Я-10…Я-20 «FamilyCore», 24.09.2026):

- Я-10, Я-11 — метаприложение планируется; итоги считает приложение, метаприложение читает готовый срез
- Я-12 — первый потребитель среза — метаприложение; обзор недели «Делу Время» даёт на него ссылку и срезов соседей не читает. Это наша рекомендация из вычитки
- Я-13, Я-14 — деньги ступенями; свободный текст человека в срез не идёт никогда — заметки, пункты плана, повторы с текстом
- Я-15 — потребитель показывает, а не досчитывает: ни серий, ни средних на день, ни цвета, ни вердикта сверх данного. Держит в договоре Р-05, Р-45, Р-55 отсюда
- Я-16 — срез — `summary.json` в корне репозитория данных, пишет проход синхронизации, без `settings` устройства
- Я-17 — форма общая: показатель `key`, `label`, значение, `basis`; четыре отрезка — прошлая и идущая неделя, прошлый и идущий месяц; `through` у идущего
- Я-18, Я-20 — «требует внимания» — только закончившееся, с основанием: у нас — обзор прошлой недели не проведён; тема `day` остаётся нашему напоминанию
- Я-19 — состав «Делу Время»: минуты по группам, фон отдельно, основание из `elapsedDays`; счёты плана и факта без текстов; нормы недели вердиктом

Место, путь и форма среза решены; код среза в ядре — `d049c2c`, `9a956c6`.

**Сделано здесь**

- `CLAUDE.md`, «Как работать», п. 9 — перед новой функцией или правкой итогов сверяться с «Срезом итогов» договора; противоречие — в сессию ядра
- **Р-86** — путь интеграции — метаприложение; закрывает открытый вопрос `05` и условие Р-50 «через четыре недели обзора»
- План, «Отложено» — таблица путей и набросок заменены: что из находок вычитки ждёт здесь и когда; порядок фактов соседей — для метаприложения
- `01-Проект`, «Модуль 4» — строка о фактах недели

**Не описано в документах — на обсуждение**

- Первый промпт ядра писался до Я-16…Я-20 и называл место и форму нерешёнными; выполнен по нынешнему состоянию ядра, заголовок раздела договора — «Срез итогов», а не «Срез итогов — строится»
- Прежние — в записи ниже

**Следующий шаг**

Срез итогов здесь — по второму промпту ядра: указатель ядра, суммы по группам чистой функцией, `summary(data, day)`, решение о ключах и подписях. План работ — человеку до кода.

---

## 24.09.2026 — Строка из перевода «Дневников»: семья на ядре целиком

**Запись сделана не отсюда**, а сессией перевода «Дневников» из их корня,
по прямому разрешению человека. Тронут только этот Журнал.

«Дневники» переведены на `FamilyCore` `897f3ae` (Р-81…Р-86 «Дневников»),
последними — теперь на ядре все четыре приложения. Для «Делу Время» кода
нет. Общее, что пригодится здесь:

- **Скрипт сверки вёрстки** (`layoutdiff.mjs` из вашей сессии перевода)
  сработал у «Дневников» после подгонки адреса и маршрутов — и нашёл
  поломку, которую разбор стилей пропустил. Вопрос «взять ли его в
  `scripts/` или в ядро» из вашего Журнала теперь про две семьи
- **Свежие данные для прогона — из самого репозитория данных:** клон только
  для чтения, слепок собирается из его файлов. Сильнее файла-копии и не
  требует её снимать; заменил бы синтетический слепок вашего перевода

---

## 24.09.2026 — Перевод на общее ядро `FamilyCore`: машинно всё сошлось, влито в `main`; телефон — за человеком

Сессия по передаче Б3 «Трапезы» (Р-49 «Трапезы»: «Трапеза» → «Злотые» → «Делу Время» → «Дневники»). Ветка `familycore`, влита fast-forward.

**Решения — записаны в момент выбора**

План работ, развилки и оба мелких вопроса — по умолчанию; человек: «план одобряю, по решениям и вопросам тож ок».

- **Р-83 — перевод:** подмодуль `src/shared`, один коммит-переезд, данные не меняются; телефон — после вливания. Прогон на копии настоящих данных — не в этот раз: человек копию снимать не стал, «если что, через гит откатим, критических данных там нет»
- **Р-84 — тесты ядра:** `vitest` не гоняет `src/shared`, `tsc` проверяет. Как Р-52 «Трапезы»
- **Р-85 — голый «Р-NN» в своих файлах — наш,** чужой — с хозяином. Как Р-53 «Трапезы»
- Строки «Что нового» нет — перевод не виден; сверка вёрстки двух сборок — да

**Сверка до кода — что нашлось сверх передачи**

- `BarChart` и `Report` в передаче названы своими экранами — они уже в ядре (`ui/BarChart`, `bars`, `Report`, `report`). Своими остались `Inbox`, `Month`, `Year`, `Review`
- Код ядра на `d12def9` против `d86f0aa`: `dates`, `github`, `id` — только хозяева номеров в комментариях; `db`, `sync`, `layout`, `importing` — фабрики с тем же API для модулей; поведение, новое для нас, — только Я-07 (общий абзац импорта) и Я-08 (`getByIndex`, не используется). Каркас стилей — ровно наши правила без двенадцати кусков; работник — наш код, обёрнутый в `startWorker`
- Отличия от «Трапезы»: хранилища первого запуска — `notes`, `time`, `reviews`, не все (`OWN_STORES` в `app/model.ts`); вкладки — из своих названий (Р-26), обёрткой `Shell` над `Layout` ядра; напоминаний две темы (Р-51); `share_target` — через `manifest` фабрики

**Сделано**

- `270b03d` — Р-83…Р-85
- **Коммит-переезд.** Подмодуль `src/shared` на `d12def9`, `submodules: true` в `deploy.yml`. `src/app/model.ts` (из `core/model.ts`, без `Base`, `Migration`, `LOCAL_STORES`), `config.ts`, `core.ts`, `config.test.ts`. Удалено 48 файлов копии; импорты в 81 файле — скриптом. `app.tsx` — `CoreProvider`, `Shell`, `sync.startAutoSync`; `main.tsx` — каркас стилей первым, `listenErrors(db.settings)`. `registry` — `Data` и `Plan` по таблице хранилищ, `importing.readImportFile` и `buildPrompt`, `kindLabel`; разделы модулей — `ImportPlan<StoreRecord>`. `notify.ts` — две темы поверх `createReminders`, `notify.test.ts` — только сторож имён ключей; `sw.ts` — `startWorker`; `changes.ts` — только `CHANGES`. `styles.css` — свои 95 правил и свой `:root` с акцентом обеих тем. `vite.config.ts` — `familyVite`; `tsconfig` — `extends`; `icons.mjs` — `writeIcons`. Прогон — заголовок README данных
- **Коммит хозяев чужих номеров (Р-85) не понадобился.** В своих файлах чужих голых номеров не осталось: всё, что несло номера «Дневников», ушло с копией ядра. Сверено по теме с `05` — в своей части `styles.css` все 12 ссылок наши (их же «Трапеза» пометила «Делу Время»), обвязка прогона несёт только Р-11, `Settings`, `Feed`, `launch`, `app`, `main`, названия вкладок — только наши
- Документы: `CLAUDE.md` — правила ядра; Архитектура — дерево с `src/shared` и `src/app`, «Как добавить модуль», договор семьи ссылкой на ядро, три риска подмодуля; `06-Процесс` — «Подмодуль ядра», «Как читать Р-NN», порча с подмодулем, передача; План — хвосты перевода

**Данные не тронуты.** `deluvremya`, версия 1, шесть хранилищ, `v1Stores`, индексы `notes` (`capturedOn`, `plannedFor`), `time` (`date`), `reviews` (`weekStart`), раскладка `DeluVremyaData`, `deluvremya-import` — буквально прежние; миграций нет. Сами поменялись тексты ядра: README нового репозитория данных — «# Данные приложения «Делу Время»» (лежащий не переписывается, Р-69), ошибки слепка и импорта — «приложения «Делу Время»», общий абзац импорта (Я-07 «FamilyCore»).

**Нехватки ядра нет.** `<CoreProvider value={{ config, db, sync }}>` принял наши `db` и `sync` без единого `as` — починка `SharedDb` после «Трапезы» (`d1f282f` «FamilyCore») сработала и у второго приложения.

**Состояние: перевод влит, машинно проверен; телефон — за человеком**

*Проверено машинно* (итоги по коду выхода)

- `tsc --noEmit` — 0, с `src/shared` и его тестами; `npm test` — 0, 31 файл, 449 тестов (было 722: механика ушла с ядром, добавлено 10 в `app/config.test.ts` и 2 сторожа ключей напоминаний)
- `npm run build` — 0; в `dist/sw.js` манифест прекэша вписан, 13 записей, строки `__WB_MANIFEST` нет. Предупреждение `inlineDynamicImports` было и до перевода
- `npm run smoke` — 0, 144 проверки, ошибок нет; в нём работа без сети и синхронизация на подставном GitHub
- `npm run smoke -- --data <синтетический слепок>` — 0, 13 проверок: 492 записи через «Восстановить из копии», одиннадцать экранов со всеми развёрнутыми блоками. Слепок собран в папке сессии из февральского импорта `seed/` плюс шесть недель учёта, заметки всех видов, план, шаблон, нормы, обзор — **настоящих данных нет**, по слову человека
- **База, заведённая прежним кодом** (так же, как прежний `createStores`), открывается ядром с записями, настройкой синхронизации и очередью `dirty` — тест в `app/config.test.ts`
- **Промпт импорта — байт в байт прежний** (разовое сравнение до и после); README данных — отличается только текстом ядра
- **Манифест** — по значениям совпал с прежней сборкой, с `share_target` и тремя ярлыками; иконки через `writeIcons` — четыре PNG байт в байт прежние
- **Стили:** 71 правило каркаса + 95 своих = прежние 166; своих блоков, стоявших раньше каркасного с тем же селектором и свойством, — ноль
- **Вёрстка — сверкой двух сборок:** прежняя и новая, тот же слепок, окно 390×844, всё развёрнуто, у каждого элемента — место и 31 вычисленное свойство. Десять экранов из одиннадцати совпали целиком (1945 элементов); в «Настройках» — только блок «Импорт записей»: своя фраза и общий абзац ядра — два абзаца вместо одного, блок на 94 px выше, остальное ниже сдвинуто; расхождений стиля — ноль
- поиск `indexedDB` вне `src/shared/core/db.ts` и тестов — пусто; `git status` чистый, в `src/shared` тоже

*Проверено на устройстве* — нет, ждёт человека: установленное приложение на телефоне без переустановки.

*Не проверено* — прогон на копии настоящих данных; второе устройство. Хвостами в Плане.

**Находка для перевода «Дневников» — перевод упрётся в ядро**

Сверкой `a913dcb` «Дневников» (`git show`, только чтение): у них `cycleEvents` и `content` лежат по годам — `split: 'year'`, `cycles/ГГГГ.json`, `content/ГГГГ.json`. `Place` ядра и договор семьи знают только «одним файлом» и «по месяцам». Без изменения данных «Дневники» не перевести, пока ядро не умеет годовую нарезку, — это правка договора, решение Я-NN «FamilyCore», сессией сопровождения **до** их перевода. Второе: лента ядра ставит записи без даты вниз (Р-61), у «Дневников» — наверх, как поломку; их перевод это поменяет, если не решить отдельно. Оба пункта — в промпте перевода «Дневников» (`06-Процесс`).

**Ошибки по ходу, мои**

- `node -e` в Bash съел обратную косую черту в регулярке — ровно то, о чём предупреждала передача. Скрипт упал до записи; дальше — только скриптами-файлами из папки сессии
- Скрипт путей сперва не знал `export … from` — упал на первом же реэкспорте до записи, правило дописано
- В плане назвал 50 удаляемых файлов — в `src/core` было 17 с `model.ts`, а не 18; удалено 48

**Что сказал человек по ходу**

- Копию данных снимать не будет: «если что, через гит откатим на пред версию, критических данных там нет»
- После деплоя: «на тф пока мне кажется деплой не обновился», «Что нового» не вышло. Второе — так и задумано: строки нет (Р-83). Первое — работник подхватывает сборку при следующем запуске; признак — дата сборки в «Настройки» → «О приложении»
- Строки для Журналов ядра и «Трапезы» — записать отсюда, «в порядке исключения»: записаны, тронуты только их `04-Журнал.md`

**Не описано в документах — на обсуждение**

- **Сверка вёрстки двух сборок** — скрипт в папке сессии (`layoutdiff.mjs`) и генератор слепка (`synth.mjs`). В репозиторий не положены. Для «Дневников» пригодились бы; взять ли в `scripts/` здесь или в ядро — вопрос
- **Предупреждение про один импорт на одном устройстве** (Р-21 «Трапезы») касается и нас: импорт заметок и учёта даёт случайные id. Вопрос ядра, открыт там
- Прежние: группы только в кнопках и итогах; две классификации категорий; порядок плана при перестановке на двух устройствах; текст README в заведённых репозиториях сам не обновляется; ошибки service worker — не в журнале; «Открыть на GitHub» с телефона — нужен вход; нормы за год только строкой; хвосты — в Плане

**Следующий шаг**

Человек открывает установленное «Делу Время» на телефоне **без переустановки**: прежние записи на месте, синхронизация идёт, «Настройки» → «О приложении» — дата сборки новая. Не открылось — `git revert` коммита-переезда (Р-83). Дальше по семье: **сессия сопровождения `FamilyCore`** — годовая нарезка и лента (промпт — `06-Процесс`, «Сопровождение `FamilyCore` перед переводом «Дневников»»), **затем перевод «Дневников»** сессией из их корня (промпт там же, следом). Здесь — по передаче: разбор issues, открытые вопросы.

---

## 14.09.2026 — Доведение до ума закрыто: на телефоне работает, Р-82 подтверждено

**Решения**

- Р-82 — порог сворачивания в 20 записей — подтверждён человеком: «с решением согласен». Новых записей в `05` нет
- Порча — в отдельной рабочей копии `git worktree`: человек — «ок, если это не сильно увеличит расход токенов». Правило в `06-Процесс`, проверяется при первой порче
- Интеграции семейства — после готовности всех приложений; человек: «в итоге будет одно мета приложение». Пятый путь и набросок содержимого — План, «Отложено»; в `05` — открытым вопросом
- Признак у группы, а не у категории — мысль человека «на будущее»; открытым вопросом в `05` с рекомендацией пожить неделю с группами
- Сколько друзей: ограничения в архитектуре нет, пределы практические — План, Этап 7

**Состояние: доведение закрыто**, с хвостами, как этапы до него

*Проверено на устройстве* — со слов: «На тф всё работает»

*Проверено машинно*

- Прогон экранов на свежей копии настоящих данных (Р-68): `deluvremya-2026-09-14.json` из «Загрузок» человека, «Загружено записей: 208», одиннадцать экранов со всеми развёрнутыми блоками, в том числе итоги месяца и года, — 13 проверок, ошибок нет. На копии уже стоят группы человека («Хобби»). Копия в репозиторий не попадала, профиль браузера удалён
- Issues в репозитории кода включены, открытых и закрытых нет — по API GitHub. Закрывает вопрос Этапа 6: «Открыть на GitHub» в отчёте об ошибке ведёт на работающую форму

*Не проверено*

- README в `DeluVremyaData`, второе устройство, порог сворачивания на выросшем списке — хвостами в Плане

**Находки по ходу**

- Человек спросил, не пушится ли порча в `main`. Не пушится и не коммитится: она ложится в рабочие файлы, прогон идёт на локальной сборке во временном профиле браузера, откат — `git checkout -- src`, пуш — после проверки, что `src` совпадает с коммитом. Слабое место одно: обрыв сессии между порчей и откатом оставил бы поломку в рабочей папке, и следующий `git add -A src` её бы захватил. Порча в отдельной рабочей копии, `git worktree`, закрывает и это

**Не описано в документах — на обсуждение**

Нового нет: порча в отдельной рабочей копии согласована, интеграции и признак у группы — открытыми вопросами в `05`.

Переходят из прошлой записи: группы только в кнопках и итогах, без ленты, markdown и норм группы; две классификации категорий — группа и признак; порядок плана при перестановке на двух устройствах; текст README в заведённых репозиториях сам не обновляется; ошибки service worker — не в журнале; «Открыть на GitHub» с телефона — нужен вход; нормы за год только строкой; минуты категорий по месяцам года — в подсказке; столбец идущего месяца — как законченный; повторы пунктов за год — длинный список; обзор и напоминание при двух устройствах; без связи точка красная; хвосты — в Плане.

**Следующий шаг**

С человеком: что из открытых вопросов брать, или следующее приложение семьи. Интеграции — после всего семейства, метаприложением (набросок в Плане). Отдача другу — вместе с семейством (Р-67).

---

## 14.09.2026 — Доведение до ума: двенадцать пунктов и README данных написаны, ждут проверки на телефоне

**Решения — все записаны в момент выбора**

Предложены в плане работ с умолчаниями, человек: «С планом соглы… приступай»; разделение Этапа 7 — «сам реши как лучше». По ходу обсуждения плана он добавил группы категорий с отражением в аналитике и перенос пункта плана (`4afe334`).

- **Р-68 — из Этапа 7 сейчас — прогон экранов на копии данных и README данных;** остальное — к отдаче
- **Р-69 — README репозитория данных кладёт приложение, если его там нет.** Отступление от ядра «Дневников»
- **Р-70 — стрелки «в начало» и «в конец» из «Дневников»; жеста между вкладками нет.** Пункт Плана был записан моей догадкой
- **Р-71 — ссылки внизу «Сегодня» — блок «Итоги и история» карточками**, с итогами года
- **Р-72 — реализм — плашкой под заголовком «План»**
- **Р-73 — «На сегодня» и «На завтра» при записи дела**
- **Р-74 — «неразобранное» склоняется; Р-26 — о названиях вкладок**
- **Р-75 — порядок пунктов плана — поле `Note.order`.** Правка модели
- **Р-76 — «на сегодня» отдельно от «впереди»; время обзора — из константы**
- **Р-77 — неделя — полем даты, месяц и год — списком**
- **Р-78 — месяцы ленты и неразобранного сворачиваются; поиск раскрывает**
- **Р-79 — markdown — разделы и период на выбор.** Меняет Р-63
- **Р-80 — перенос пункта плана — кнопкой ↷ в строке**
- **Р-81 — группы категорий — поле `Category.group`; кнопки и итоги по группам.** Правка модели
- **Р-82 — месяцы сворачиваются по умолчанию только в длинном списке, больше 20 записей.** Уточняет Р-78. Решено по ходу работы, после одобрения плана, — **отступление, на подтверждение человеку**

**Сделано**

- README данных: таблица файлов собирается из раскладки `core/layout.ts`, проход кладёт README, если его нет, и не трогает, если есть; прогон на копии обходит итоги месяца и года (`ff9f872`)
- Стрелки прокрутки: `ui/ScrollButtons.tsx` и стили с `a913dcb` байт в байт отдельным коммитом, хеш блоба совпал (`4df4195`); подключение в `ui/Layout.tsx` (`8682d28`)
- «Итоги и история» — четыре карточки со своими SVG-значками; реализм плашкой; ↷ в строке пункта и «Перенести» в карточке; «неразобранное» склоняется; «на сегодня» в плане против факта; `REVIEW_MINUTES` (`6f15ed4`)
- «На сегодня» и «На завтра» при записи дела (`07dceba`)
- Группы категорий: `modules/time/groups.ts` с тестами; экран категорий с заголовками групп, ↑↓, «Переименовать группу», «Убрать группу», выбор группы в карточке; «Категории и группы» в «Настройках»; кнопки «Учёта» и «Сегодня» сворачиваемыми группами с минутами за день; строки групп в итоге дня, времени недели и месяца, итогах года; слияние одноимённых переносит группу; стартовый набор с группами (`12f5617`)
- Месяцы ленты и неразобранного сворачиваются; `?open=` раскрывает месяц своей записи и «Замыслы» (`b53310f`)
- Неделя — полем даты, месяц и год — списком; markdown — разделы и период; `Note.order` с «↑ Выше» и «↓ Ниже», стрелки шаблонов (`728e006`)
- «Что нового» — запись 18, строка на каждое видимое изменение

**Находки по ходу**

- Листания экранов в «Дневниках» нет — там `ui/ScrollButtons`, стрелки прокрутки. Пункт Плана записан моей догадкой «жестом», человек поправил
- Перенос пункта плана был с Этапа 4 — в карточке, и его не нашли. Отклик, которого не видно, — половина отклика: так же было с реализмом на Этапе 4
- Порядок групп выводится из порядка категорий, и группа, у которой ушла первая категория, съезжала вниз. Правки группы раскладывают теперь по уже показанному порядку. Поймали тесты до экрана
- Одна группа двумя написаниями меняла название при перестановке категорий. Выбор и набор группы пишут написанием существующей
- Прогон на копии не открывал итоги месяца и года: Этап 8 их в обход не внёс
- Проверка «Что нового» ждала текст записи 17 и падала бы на каждой новой записи. Две последние записи прогон берёт теперь из `src/changes.ts`
- Короткий список со свёрнутыми старыми месяцами прятал бы висяки за лишним тапом — отсюда Р-82

**Ошибки по ходу, мои**

- В плане работ заявил, что листания в «Дневниках» нет: искал касания и свайпы, а там стрелки. Ловится одним: искать по тому, что человек описал словами, а не по своей догадке о механизме
- Шаг сценария ↷ начал со скобки `[...]` — склеился с помощниками, `SyntaxError`, клик не случился, прогон упал. Та же грабля записана Журналом Этапа 3 — наступил снова. Шаг начинается с `const`
- Скрипт правок групп в heredoc bash разобрал неверно («unexpected EOF») — не легло ничего, заметил по ошибке. Большие правки — файлом в папке сессии
- Лишний импорт `UNSORTED_TITLE` после правки текста — поймал `tsc`

**Состояние: код написан, доведение не закрыто**

*Проверено машинно*

- `tsc` — 0; `vitest` — 722 теста в 46 файлах
- Прогон — 144 проверки, ошибок нет, на конечном коде `728e006`; все 14 новых проверок доведения сошлись. Прогоны по порциям: после `6f15ed4` — не сошлось 2 из 137 и ошибка браузера, обе причины в сценарии (склейка шага со скобки и устаревшая проверка «Что нового»); после `12f5617` с починкой — 139, ошибок нет
- Порча — один прогон с двенадцатью порчами разом, код 1: не сошлись все 13 новых проверок, на которые она рассчитана (порча карточки года валит две), и ни одна не прошла. Сверх них — три старые проверки Этапа 4: «На завтра» из карточки и две проверки шаблонов после неё. Причина — общая: кнопки переноса в карточке и под ↷ теперь один блок `MoveButtons` (Р-80), и порча ↷ сломала и перенос из карточки; шаблоны упали следом, пункт остался на сегодня. Остальные 128 прошли, ошибок браузера нет. Порча откачена из коммита, `src` совпадает с `728e006`
- Перед пушем `npm test` и `npm run build` — код 0. Пуш двумя частями: до `12f5617` — после чистого прогона на нём, остальное — после чистого прогона и порчи на `728e006`. Деплой: в скрипте живого сайта `assets/index-BIcZMhGf.js` — `md-period`, «↑ Выше», «Итоги и история», «В новую группу», «Данные «Делу Время»»; в стилях — `link-card`, `scroll-btns`, `stats__group`

*Проверено на устройствах*

- Ничего

*Не проверено*

- Всё «Что проверить на телефоне» — План, «Доведение до ума»
- Прогон экранов на свежей копии настоящих данных — файла копии нет
- README в `DeluVremyaData` — появится при первой синхронизации обновлённой сборки
- Стрелки шаблонов — тестом, не прогоном: у прогона один шаблон
- «README человека не трогается» и раскрытие «Замыслов» из ленты — прогоном без порчи: порча одна на строку

**Не описано в документах — на обсуждение**

- **Р-82 — отступление от одобренного плана:** порог сворачивания — 20 записей. Подтвердить или вернуть «старые месяцы свёрнуты всегда»
- **Группы — только кнопки и итоги.** Лента и markdown — по категориям, нормы у группы нет (Р-81)
- **Две классификации категорий** — группа и признак `kind`. Если при группах признак окажется лишним — отдельным решением
- **Порядок плана на двух устройствах:** перестановка одного дня на обоих до обмена перемешает порядок; пункты не теряются
- **Текст README в заведённых репозиториях сам не обновляется** (Р-69)

Закрыто этой записью: карточка замысла из ленты в свёрнутом блоке (Р-78), markdown «всё и за всё время» (Р-79); «лента рисует всё разом» — сворачиванием длинной ленты (Р-82). Переходят: ошибки service worker — не в журнале; «Открыть на GitHub» с телефона — нужен вход в GitHub; нормы за год только строкой; минуты категорий по месяцам года — только в подсказке; столбец идущего месяца — как законченный; повторы пунктов за год — длинный список; обзор и напоминание при двух устройствах; без связи точка красная; хвосты Этапов 3–6 и 8 — в Плане.

**Следующий шаг**

Проверка на телефоне по Плану, «Доведение до ума», «Что проверить»; файл копии данных для прогона экранов. По ответам — закрыть доведение. Отдача другу — вместе с семейством (Р-67).

---

## 14.09.2026 — Этап 6 закрыт: лента, выгрузка, справка на телефоне работают; отдача другу отложена

**Решения**

- **Р-67 — отдача другу — вместе с семейством приложений.** Человек: «пока другу передавать не буду — передам, когда будет готово уже всё семейство приложений». Что из Этапа 7 делать сейчас — в начале следующей сессии; моя рекомендация — прогон экранов на свежей копии настоящих данных и README для репозитория данных, остальное и iPhone — к отдаче
- Отложено человеком в «доведение до ума», План, п. 7–10: ссылки внизу «Сегодня» — отдельным блоком, одна под другой; листание экранов жестом, как в «Дневниках»; сворачивание по месяцам и разделам в ленте и где уместно; выбор, что выгружать в markdown — меняет Р-63, новым решением при реализации
- `01-Проект`, «Распространение»: отдача другу — вместе с семейством (Р-67)

**Состояние: этап закрыт**, с хвостами, как Этапы 1–5 и 8

*Проверено на устройстве* — со слов: «Всё появилось, работает»

*Не проверено*

- Лента на году данных, второе устройство — хвосты Этапа 6 в Плане

**Находки по ходу**

- В локальной копии «Дневников» на `a913dcb` листания экранов нет — видимо, появилось у них позже. Пункт записан по словам человека; их свежий код — смотреть при реализации, только читая

**Не описано в документах — на обсуждение**

Нового нет. Переходят: лента рисует всё разом (теперь и «доведение до ума», п. 9); карточка замысла из ленты в свёрнутом блоке; ошибки service worker — не в журнале; «Открыть на GitHub» с телефона — нужен вход в GitHub; нормы за год только строкой; минуты категорий по месяцам года — только в подсказке; столбец идущего месяца — как законченный; повторы пунктов за год — длинный список; обзор и напоминание при двух устройствах; без связи точка красная; хвосты Этапов 3–6 и 8 — в Плане.

**Следующий шаг**

Новой сессией: решение об остатке Этапа 7 (Р-67), затем «доведение до ума» — промпт в `06-Процесс`, «Передача в следующую сессию».

---

## 14.09.2026 — Этап 6: лента, выгрузка, справка написаны, ждут проверки на телефоне

**Решения — все записаны в момент выбора**

Предложены в плане работ с умолчаниями, человек: «С планом соглы, развилки 1–8 ок, приступай» (`78a0977`).

- **Р-58 — учёт в ленте — строка на день, а не на блок**
- **Р-59 — заметки в ленте — днём записи; лента только читает.** Без даты — внизу, а не наверху красным, как в «Дневниках»
- **Р-60 — обзор в ленте — днём проведения; его строки — в `screens/reviewFeed.ts`**
- **Р-61 — `core/feed.ts` копируется из «Дневников»; лента и «Заметки» ищут одним правилом.** Закрывает вопрос Этапа 3
- **Р-62 — лента — экран `#/feed`, не вкладка**
- **Р-63 — markdown — раздел на вид, внутри месяцы и дни; реестр полный.** Рост реестра по Р-23 закончен
- **Р-64 — справка `#/help`, «?» на «Сегодня»; числа из констант**
- **Р-65 — «Что нового» — блок на «Сегодня»; весь список — в «О приложении»**
- **Р-66 — отчёт об ошибке — техника без записей; у ошибки — путь экрана без запроса.** Отдельным решением, поэтому номеров на одно больше, чем развилок в плане работ

**Сделано**

- Из «Дневников» с `a913dcb` байт в байт отдельным коммитом (`bbe4277`): `core/feed.ts` и `ui/report.ts` с тестами, `ui/Report.tsx`, `screens/Feed.tsx`, `useFeed.ts`, `WhatsNew.tsx`, `useWhatsNew.ts`. На этом коммите сборка не проходит — правки следующими
- Ядро ленты по делу проекта: `RecordKind`, без даты — внизу, внутри дня — по виду в порядке реестра, дата словами в поиске; «Заметки» ищут правилом ядра (`bc912d1`)
- Строки ленты и markdown: `modules/notes/feed.ts`, `modules/time/feed.ts`, `screens/reviewFeed.ts`; в реестре лента и markdown обязательны; февраль в markdown — 211 ч 50 мин, строка на каждый из 28 дней (`3f8c439`)
- Лента `#/feed`, ссылка внизу «Сегодня»; «Заметки» раскрывают карточку по `?open=` и прокручивают к ней (`21f0cfe`)
- Markdown — «Настройки» → «Экспорт и импорт» → «Markdown для чтения» (`78772a2`)
- «Что нового» на «Сегодня», весь список — в «О приложении» (`f395c23`)
- Отчёт об ошибке: журнал `errorLog`, ловля ошибок с `main.tsx`, раздел в «О приложении» (`0f0d006`)
- Справка `#/help`, «?» на «Сегодня», сторож цифр `help.test.ts` на справку и приветствие; приветствие без «Обзор недели появится следующими обновлениями» (`a862485`)
- Прогон: сценарий Этапа 6 — девять проверок; лента и справка — в прогоне на копии данных
- «Что нового» — запись 17

**Находки по ходу**

- `errorLog` был только в Архитектуре — в код не писался; `seenChanges` не читал никто: окна не было
- Приветствие два этапа обещало обзор недели «следующими обновлениями». Сторож цифр такое не ловит — ловит теперь проверка прогона
- Отчёт «Дневников» пишет адрес экрана целиком. Здесь в нём бывает текст из «Поделиться» — `#/inbox?shared=…`, — и личный текст ушёл бы в публичный issue. Путь — без запроса (Р-66)
- В «Дневниках» запись с нечитаемой датой — поломка, и лента ставит её наверх красным; здесь без даты — законно (Р-08). Правило ленты перевёрнуто
- Кнопки markdown — «Сохранить markdown» и «Поделиться markdown»: «Сохранить в файл» и «Поделиться» уже есть у копии, и прогон, нажимающий по тексту, их бы спутал
- Отправка файла в «Настройках» уже меняла `.md` на `.txt` для «Поделиться» — markdown с телефона уходит без новой механики
- Прогон шлёт ошибку страницы событием `ErrorEvent`, а не настоящим исключением: исключение само валит прогон. «Что нового» на копии без прочитанного прогон проверяет, стирая `seenChanges` прямо в IndexedDB, — это сценарий, а не код `src`

**Ошибки по ходу, мои**

- Порча строк обзора оставила `firstLine` без дела, и `tsc` на ней упал — поймано `tsc` на переднем плане до долгого прогона, как записано после Этапа 8. Порча переписана: значение пустеет, функция остаётся в деле
- В справке сперва стоял пример даты «12.03.2026» — цифрами в исходнике справки. Заметил, перечитав до тестов; сторож поймал бы тоже
- Проверку «личного текста в отчёте нет» написал по слову, которое в адресе стоит закодированным: проходила бы всегда. Нашла порча. Ловится одним: проверка «этого нет» ищет то, что туда попало бы, а не то, что туда положили

**Состояние: код этапа написан, этап не закрыт**

*Проверено машинно*

- `tsc` — 0; `vitest` — 687 тестов в 43 файлах; прогон — 130 проверок, ошибок нет
- Порча — один прогон с девятью порчами разом, код 1 ровно на девяти новых проверках: без даты — наверху, строка дня ведёт на «Учёт» без дня, у обзора нет наблюдения, `?open=` не раскрывает карточку, markdown без шапки, «Что нового» не показывается, в журнал — адрес целиком, «?» под другой подписью, приветствие не зовёт в справку. Остальные 121 прошли, ошибок браузера нет
- Порча нашла слабость проверки отчёта: «личное» из «Поделиться» в адресе закодировано, и «нет слова „личное“» проходило бы всегда. Проверка — по параметру `shared`
- Чистый прогон конечного кода — после отката порчи и с исправленной проверкой отчёта: 130 проверок, ошибок нет
- Перед пушем `npm test` и `npm run build` — код 0. Деплой кода этапа (`bbe4277`…`1a5ed03`): в скрипте живого сайта `assets/index-CK_P8yQ9.js` — `errorLog`, «Markdown для чтения», «Справка»; в стилях `assets/index-BVeId5z4.css` — `feed__row`

*Проверено на устройствах*

- Ничего

*Не проверено*

- Всё «Что проверить на телефоне» Этапа 6 — План. Включены ли Issues в репозитории кода — от этого зависит «Открыть на GitHub»

**Не описано в документах — на обсуждение**

- **Лента рисует всё разом.** На году данных — около 365 строк учёта, заметки и пункты-привычки из шаблонов: сотни строк одним списком. Начнёт тормозить на телефоне — рисовать по месяцам
- **Карточка из ленты в свёрнутом блоке «Замыслы»** раскрывается, но не видна: блок остаётся свёрнутым, если его свернули раньше
- **Markdown — всё и за всё время**: ни разделов, ни периода на выбор (Р-63). На году данных файл вырастет, но читается по месяцам
- **Ошибки service worker** в журнал не попадают (Р-66); фоновая проверка ведёт свой журнал пробуждений
- **«Открыть на GitHub» с телефона** ведёт на форму issue, для которой нужен вход в GitHub в браузере телефона; без входа — «Поделиться» или «Скопировать»

Переходят из прошлых записей: нормы за год только строкой; минуты категорий по месяцам года — только в подсказке; столбец идущего месяца — как законченный; повторы пунктов за год — длинный список; обзор и напоминание при двух устройствах; без связи точка красная; хвосты Этапов 3–5 и 8 — в Плане.

**Следующий шаг**

Проверка на телефоне по Плану, Этап 6, «Что проверить». По ответу — закрыть этап, затем Этап 7 «Отдать другу».

---

## 14.09.2026 — Этап 8 закрыт: итоги месяца и года на телефоне работают

**Решения**

- Новых записей в `05` нет. Выбор любой недели в обзоре и любого месяца в итогах — человек: «добавить возможность выбора любой недели (чз календарь или как лучше?)… аналогично по месяцам — можно это отложить». В План, «После Этапа 7 — доведение до ума», п. 6, с рекомендацией: неделя — системное поле даты, как на «Учёте» (Р-27), месяц — поле месяца
- `01-Проект`, «Модуль 4»: строка «обзор месяца и итоги года — отдельным этапом» заменена тем, что сделано (Р-54, Р-55, Р-57)

**Состояние: этап закрыт**, с хвостами, как Этапы 1–5: остальное проверяется жизнью

*Проверено на устройстве* — со слов: «Все норм», «всё работает»

*Не проверено*

- «Готово когда» по октябрю — жизнью, в обзоре недели 26 октября — 1 ноября; история нормы с отметками — через три полных недели; второе устройство — отдельно не названо. Всё — хвостами Этапа 8 в Плане

**Не описано в документах — на обсуждение**

Нового нет. Переходят: нормы за год только строкой; минуты категорий по месяцам года — только в подсказке; столбец идущего месяца — как законченный; повторы пунктов за год — длинный список; обзор и напоминание при двух устройствах; без связи точка красная; хвосты Этапов 3–5 — в Плане.

**Следующий шаг**

Этап 6 «Лента, выгрузка, справка» новой сессией — промпт в `06-Процесс`, «Передача в следующую сессию».

---

## 14.09.2026 — Этап 8: итоги месяца и года написаны, ждут проверки на телефоне

**Решения — все записаны в момент выбора**

Предложены в плане работ с умолчаниями, человек: «Согласен с планом, приступай» (`4a06e54`).

- **Р-54 — итоги месяца — экран без записи; вход из обзора недели, закрывающей месяц.** Закрывает вопрос Р-52. Ритуал с записью описан там же как открытый путь без миграции
- **Р-55 — месяц: время против прошлого месяца, нормы по неделям, план против факта.** Неделя — месяцу своего воскресенья
- **Р-56 — история нормы — полные недели с `since`; правка правил день не сдвигает.** Уточняет Р-53
- **Р-57 — итоги года — столбцы месяцев своим SVG, категории строками**

**Сделано**

- Месяцы в `core/dates`: месяц дня, промежуток месяца, сдвиг, недели по воскресенью, месяц, который неделя закрыла (`df4ca48`)
- История нормы: `since` в модели и в форме нормы, одно правило истории на неделю, месяц и год; пока недель мало — «норма с … · история — с 3 полных недель, пока N» (`0f2bd71`)
- Блоки обзора на промежутке: `PlanPeriod`, `PeriodTime` со вторым столбцом, `PeriodNorms` (`1b15ded`)
- Итоги месяца: экран, карточка «… закончился» в обзоре недели, ссылка внизу «Сегодня» (`e637922`)
- График столбцами в `ui`: геометрия чистыми функциями с тестами, SVG без библиотек (`b42234e`)
- Итоги года: столбцы месяцев, «Месяцы таблицей», категории с малыми столбиками, нормы и план за год (`01c6d70`)
- Тест итога месяца на настоящем феврале (`3a04d06`); прогон — месяц, год, стык, история нормы (`8c69a6a`)
- «Что нового» — записи 14–16

**Находки по ходу**

- Тёмный акцент `#f2b35b` для столбцов не прошёл проверку скриптом палитры: светлота 0,81 при потолке 0,67 для тёмного фона. Взят `#c9822b` той же гаммы — переменная `--chart`. Светлый акцент прошёл как есть
- Расчёты Этапа 5 принимали промежуток, а компоненты — неделю: перевод на промежуток — переименование без новой логики
- Для теста февраля `node:fs` не годится — типов Node в проекте нет. Взят `import.meta.glob`: пустой glob и есть «файла нет», на сборке GitHub тест пропускается
- `db.put` ставит `updatedAt` на каждой правке, поэтому запасной день нормы без `since` сдвигается при переименовании и перестановке категории. Р-53 это допускает; первая правка нормы записывает день в `since`
- Дни месяца после его последнего воскресенья входят во время месяца, а их неделя — в нормы следующего. Сказано в основании блока норм (Р-55)
- Порча нашла слабость сценария: шаг года начинался кликом со страницы, куда вёл клик по карточке «Июль закончился». Без карточки падали и проверки года — `columns: 0` на экране обзора, — и порча столбцов года оставалась непроверенной. Шаг года теперь открывает итоги июля переходом

**Ошибки по ходу, мои**

- `git add` со списком, где стоял старый путь после `git mv`: add упал целиком, и коммит ушёл с одним переименованием — на нём сборка ломалась. Коммит не был запушен, дополнен тем же набором через `--amend`. Ловится одним: после коммита — `git show --stat`, а не только `git log -1`
- Порча дважды не собралась, и прогон с ней не запускался: `false` в цепочке `&&` перед JSX TypeScript не пропускает — сужение `compare` и `before` теряется, — и ставил его в двух местах цепочки подряд, не проверив. Каждый раз — сборка в фоне вместо мгновенного `tsc`. Порча теперь меняет значение, а не условие. Ловится одним: наложил порчу — сперва `tsc` на переднем плане, потом долгий прогон

**Состояние: код этапа написан, этап не закрыт**

*Проверено машинно*

- `tsc` — 0; `vitest` — 610 тестов в 38 файлах; сборка — 0; прогон — 121 проверка, ошибок нет
- Тест настоящего февраля выполнен, а не пропущен: 211 ч 50 мин, 173 блока, 28 дней — «Готово когда» по числу
- Порча — один прогон с пятью порчами разом, код 1 ровно на пяти новых проверках: строка прошлого месяца показывает этот — стык месяцев; неделя никогда не закрывает месяц — карточка «Июль закончился»; одиннадцать столбцов — год, `columns: 11`; история всегда «выполнена в» — история нормы; нет ссылки на «Сегодня» — вход. Тап по столбцу и остальные 116 прошли, ошибок браузера нет
- Чистый прогон конечного кода — после отката порчи и с развязанным шагом года: 121 проверка, ошибок нет
- Деплой кода этапа (`df4ca48`…`1ec25fc`): в скрипте живого сайта `assets/index-BrbM_YAA.js` — «Итоги года», «закончился»; в стилях `assets/index-D1xg-Bvf.css` — `--chart` и `#c9822b`

*Проверено на устройствах*

- Ничего

*Не проверено*

- Всё «Что проверить на телефоне» Этапа 8 — План. «Готово когда» по октябрю — жизнью, в обзоре недели 28 сентября — 4 октября

**Не описано в документах — на обсуждение**

- **Нормы за год — только строкой** «выполнена в N из M недель»: полсотни отметок в строку не влезают. Картина по неделям года — отдельный вид, если понадобится
- **Минуты категорий по месяцам года** — только в подсказке малых столбиков, а на телефоне подсказки нет; сами числа — в итогах месяца. Таблица категорий на двенадцать столбцов на телефон не влезает
- **Столбец идущего месяца** рисуется, как законченный: основание «учёт был в N днях из M» — в подсказке и в «Месяцы таблицей», не на столбце
- **Повторы пунктов плана за год** — список может быть длинным: привычка из шаблона — строка на весь год

Переходят из прошлых записей: обзор и напоминание при двух устройствах; без связи точка красная; хвосты Этапов 3–5 — в Плане.

**Следующий шаг**

Проверка на телефоне по Плану, Этап 8, «Что проверить». По ответу — закрыть этап, затем Этап 6 «Лента, выгрузка, справка».

---

## 14.09.2026 — Этап 5 закрыт: обзор недели на телефоне работает

**Решения**

- **Р-53 — история нормы — с дня её заведения и не раньше трёх недель.** Уточняет Р-45; ответ человека на находку «0 из 4 в первый день»: «минимальный набор записей (аля 3–4)… можешь отложить в следующие этапы». Реализация — в Этапе 8, вместе с нормами по неделям месяца; поле `since` у нормы — в Архитектуре с пометкой этапа
- «Ещё впереди» у сегодняшних пунктов и «минут на десять» в карточке обзора — человек: «будем в доведение до ума менять, пока главное функционал». План, «После Этапа 7», п. 5

**Состояние: этап закрыт**, с хвостами, как Этапы 1–4: остальное проверяется жизнью

*Проверено на устройстве* — со слов: «Всё проверил — работает»

*Не проверено*

- «Готово когда»: четыре обзора подряд, каждый меньше десяти минут, возврат к месту; напоминание об обзоре само по себе; второе устройство — отдельно не названо. Всё — хвостами Этапа 5 в Плане

**Не описано в документах — на обсуждение**

Нового нет. Переходят: обзор и напоминание при двух устройствах; без связи точка красная; хвосты Этапов 3–5 — в Плане.

**Следующий шаг**

Этап 8 «Месяц и год» новой сессией — промпт в `06-Процесс`, «Передача в следующую сессию».

---

## 14.09.2026 — Этап 5: обзор недели написан, ждёт проверки на телефоне

**Решения — все записаны в момент выбора**

Предложены в плане работ с умолчаниями, человек: «В целом согласен с планом»; порог замыслов — «пусть будет 28», но меняется; месяц и год — «решай сам» (`2b40228`).

- **Р-41 — обзор — экран `#/review`, не вкладка.** Неделя с понедельника; в воскресенье к обзору текущая, в другие дни прошлая; долг не копится
- **Р-42 — один обзор на неделю, id из понедельника**
- **Р-43 — фоновое время отдельной строкой, в сумму не входит.** Закрывает открытый вопрос
- **Р-44 — план против факта по дню плана; `doneOn` — день факта; удалённые не считаются.** Закрывает оба вопроса Этапа 4
- **Р-45 — норма — поле категории `norm`; вместо серии — последние четыре недели; на «Учёте» — только «не меньше».** Правка модели, объявлена до кода
- **Р-46 — висяки по пять за обзор; «когда-нибудь» — свой блок на «Заметках»**
- **Р-47 — замыслы без движения — строкой, без кнопок.** Закрывает вопрос Р-31
- **Р-48 — пороги обзора — настройка устройства, 30 и 28 дней.** Человек просил менять порог замыслов; порог висяков — заодно, сказано
- **Р-49 — наблюдение недели — обычная мысль; `Review.note` не используется**
- **Р-50 — интеграции с «Дневниками» в Этапе 5 нет.** Найден четвёртый путь — их база на том же origin — и его цена: `open` без их базы заводит им пустую
- **Р-51 — напоминание об обзоре той же проверкой `remind`, вс и пн, свои ключи дня**
- **Р-52 — месяц и год — Этап 8, сразу за Этапом 5; расчёты Этапа 5 — по промежутку дней.** Закрывает «Сводку за период»

**Сделано**

- Недели и промежутки в `core/dates` — своё «Делу Время» в ядре: Архитектура называла там недели, а в копии «Дневников» их не было (`0b36769`)
- Учёт: итоги за промежуток, нормы, форма нормы в карточке категории, блок «Неделя» на «Учёте» (`1dcd62b`); свёрнут по умолчанию, как в Р-45 (`3230e0e`)
- Заметки: план против факта, висяки, «когда-нибудь», замыслы без движения, возврат; блок «Когда-нибудь» на «Заметках» (`9170d0c`)
- Экран обзора, карточка и ссылка на «Сегодня», раздел «Обзор недели» в «Настройках» (`02577ad`)
- Напоминание об обзоре; «Проверить сейчас» показывает всё, о чём есть напомнить (`e23056a`)
- Прогон: сценарий обзора; проверка «Проверить сейчас» знает про дни зова; экран обзора — в прогоне на копии данных (`c0b7d60`)
- «Что нового» — записи 11–13

**Находки по ходу**

- Прогон стал зависеть от дня недели: в воскресенье и понедельник обзор зовёт, и «Проверить сейчас» показывает уведомление, а не «напоминать не о чем». Сценарий смотрит на день; дело и мысль нужной давности заводит импортом — иначе висяк и возврат проверялись бы только на старых данных
- Прогон шёл в понедельник — проверена ветка «обзор зовёт»: карточка стояла и ушла после «Обзор проведён», после обзора «обзор недели не ждёт»
- Висяки «по пять» выбираются при открытии экрана: иначе каждое решение подтягивало бы следующий, и «пять за обзор» не значило бы ничего
- План против факта идущей недели называет сегодняшние открытые пункты «ещё впереди»: день не кончился

**Ошибки по ходу, мои**

- Коммит документов через here-string PowerShell 5.1: сообщение ушло в `git commit -F -` аргументом, и git искал его как путь. Коммиты — через Bash с heredoc; записано в передачу
- Блок «Неделя» сделал развёрнутым вопреки Р-45, которое сам записал часом раньше. Заметил, дописывая План; свёрнут отдельным коммитом, итог «выполнено N из M» виден и у свёрнутого
- «Починил» помощник `flat` в сценарии, не посмотрев байты: неразрывный пробел там уже стоял. Правка не легла, вреда нет. Ловится одним: перед правкой невидимого символа — `od`

**Состояние: код этапа написан, этап не закрыт**

*Проверено машинно*

- `tsc` — 0; `vitest` — 573 теста в 35 файлах; прогон — 115 проверок, ошибок нет
- Порча — один прогон с шестью порчами разом, код 1 ровно на восьми проверках сценария обзора: обзор никогда не зовёт — первая проверка напоминаний и карточка на «Сегодня»; порог ноль проходит — пороги; восемь дней нормы проходят — норма; другие слова основания — время недели; висяков нет — висяк и «Когда-нибудь»; возврат пять недель назад — возврат. Остальные 107 прошли, ошибок браузера нет
- Чистый прогон конечного кода — после отката порчи и со свёрнутым блоком «Неделя»: 115 проверок, ошибок нет
- Деплой кода этапа (`0b36769`…`3230e0e`): в скрипте живого сайта `assets/index-DuPqeT0l.js` — `reviewStaleDays`, `reminderReviewDay`, «Обзор недели ждёт». Документы запушены вместе с этой записью

*Проверено на устройствах*

- Ничего

*Не проверено*

- Всё «Готово когда» Этапа 5 — План, Этап 5, «Что проверить на телефоне»

**Не описано в документах — на обсуждение**

- **История нормы считает недели и до того, как норму поставили.** Норма, заведённая сегодня, в прошлых неделях «не выполнена»: «выполнена в 0 из 4 недель» в первый же день. Дня заведения нормы в модели нет; поле `since` у нормы — правка модели, решать с человеком
- **«Ещё впереди» у сегодняшних пунктов** в плане против факта идущей недели. По правилу — день не кончился; читается ли — видно на телефоне
- **Обзор, проведённый на компьютере, телефон не видит до синхронизации** и напомнит — к прежнему «напоминание при двух устройствах» добавилось второе напоминание
- **Карточка обзора говорит «минут на десять»** — число словами из «Готово когда», а не из константы кода

Переходят из прошлых записей: без связи точка красная; хвосты Этапов 3 и 4 — в Плане; «Из «Неразобранное»» — в «доведении до ума».

**Следующий шаг**

Проверка на телефоне по Плану, Этап 5, «Что проверить». Четыре недели обзоров — «Готово когда». Затем Этап 8 «Месяц и год».

---

## 14.09.2026 — Этап 4 закрыт: план на телефоне, «Сегодня» остаётся отдельной вкладкой

**Решения**

- Новых записей в `05` нет. «Сегодня» рядом с «Учётом» остаётся — ответ человека на вопрос из Этапа 3: «Сегодня как отдельное окно ок». Объединять не нужно
- Отложенное человеком «до ума после того, как выполним все текущие этапы» — в новый раздел Плана «После Этапа 7 — доведение до ума»: реализм заметнее, «в план» при записи, заголовок «Из «Неразобранное»», перестановка пунктов плана. Вопросы Этапа 4 к обзору недели — человек: «пока не увижу его»; перенесены в Этап 5

**Состояние: этап закрыт**, с хвостами, как Этапы 1–3: остальное проверяется жизнью

*Проверено на устройстве* — со слов

- «План есть», пункты добавлены
- Реализм: «блок реализм не вижу», затем «увидел текст поменьше — Влезает ли день не посчитать — оценки нет у пунктов и поправил». То есть строка работает, но незаметна — в «доведение до ума»
- Кнопки «В план» у дела: «в заметках не вижу строк на сегодня/завтра… а увидел после записи». Они в карточке записи, при захвате их нет — в «доведение до ума»
- «В целом всё норм»

*Не проверено*

- Хвост прошлых дней — только завтра; шаблоны — не названы; «не влезает» ни разу не видено; второе устройство; неделя жизни. Всё — хвостами Этапа 4 в Плане

**Находки по ходу**

- Реализм без оценок — серая строка мелким шрифтом; человек его не узнал, пока не прочёл. Отклик, который не видно, — половина отклика

**Не описано в документах — на обсуждение**

Нового нет. Переходят: напоминание при двух устройствах; без связи точка красная; хвосты Этапов 3 и 4 — в Плане.

**Следующий шаг**

Этап 5 «Обзор недели» новой сессией — промпт в `06-Процесс`, «Передача в следующую сессию».

---

## 14.09.2026 — Этап 4: план дня и шаблоны написаны, ждут проверки на телефоне

**Решения — все записаны в момент выбора**

Предложены в плане работ, человек согласился на все: «пока да на всё, а там после реализации посмотрим» (`b3a82d5`).

- **Р-33 — план дня живёт на «Сегодня»**; «Учёт» — время целиком. Ответ на вопрос, зачем «Сегодня» рядом с «Учётом»
- **Р-34 — невыполненное прошлых дней ждёт решения на «Сегодня»**, вечернего ритуала нет. Отступление от `01`, объявлено до кода
- **Р-35 — реализм — оценки открытых пунктов против остатка окна дня 8–24.** Слабое место названо до кода: без пункта «Работа» в плане предупреждение почти не сработает
- **Р-36 — приоритета нет, Р-07 в силе. Р-37 — срок — это день плана, напоминания о сроке нет. Р-38 — прогресса у пункта нет**
- **Р-39 — шаблоны заводятся из плана, стартовых нет, применение без дублей по тексту**
- **Р-40 — главное одно; при двух отметках главным считается позднее**, в базу не пишется

**Сделано**

- Логика плана чистыми функциями с тестами: план дня, главное, перенос и возврат, хвост, «Впереди», сделанное вне плана, оценка, реализм; остаток окна — `windowLeft` в учёте (`1e17df2`)
- «Сегодня»: хвост прошлых дней с «Всё — на сегодня»; «План» — слот главного, поле «Добавить в план», галочка, звезда, карточка пункта (текст, оценка кнопками и полем, на сегодня / завтра / любой день, в «Неразобранное», удалить), реализм под планом; «Из «Неразобранное»» с «+»; «Впереди» по дням; «Сделано вне плана»; учёт — сворачиваемым блоком. На «Заметках» — «На сегодня / завтра / день» в карточке дела с «Отменить» и строка «Ещё N записей в плане» (`965be44`)
- Шаблоны: «Сохранить план как шаблон», применение одним тапом без дублей с «Отменить», экран `#/templates` — название, пункты, оценки, главное, порядок пунктов, удаление
- Приветствие говорит про план и про пункт «Работа» с оценкой; «Что нового» — записи 9 и 10

**Находки по ходу**

- Порции 2 и 3 плана работ слиты в один коммит: без хвоста и «Впереди» перенос «На завтра» уводил бы пункт туда, где его не видно
- Формат минут живёт в учёте времени, а модули друг о друге не знают — у заметок своя копия `durationText` в пять строк
- Название в кавычках — в именительном (Р-26), и заголовок вышел «Из «Неразобранное»», как уже было «убрано из «Неразобранное»». По правилу, но режет слух — на обсуждение
- Прогон: два клика в одном шаге сценария. После клика React перерисовывает в микрозадаче, и «Сохранить» того же шага сохранил шаблон без звезды, поставленной кликом раньше; после ввода в поле — сразу, поэтому «ввод и клик» в одном шаге годятся. Звезда и «Сохранить» разнесены, добавлена проверка звезды в сохранённом шаблоне
- Прогон вырос за десять минут и в предел команды в 600 секунд не влезает — запускается в фоне. Спячка ноутбука оборвала его без результата: процессы прогона остановлены по профилю `deluvremya-smoke`, прогон перезапущен
- `gh` нет ни в одной оболочке: деплой проверяется по скрипту живого сайта

**Ошибки по ходу, мои**

- Сценарий шаблонов — звезда и «Сохранить» одним шагом; первый прогон упал. Ловится одним: клик в шаге сценария — последнее действие шага
- Запустил сборку с прогоном на переднем плане с пределом в 600 секунд, хотя прогон плана уже шёл у самого предела. Длинный прогон — сразу в фон

**Состояние: код этапа написан, этап не закрыт**

*Проверено машинно*

- `tsc` — 0; `vitest` — 511 тестов в 31 файле; прогон — 105 проверок, ошибок нет
- Порча плана дня — два прогона, оба с кодом 1 ровно на своих проверках: «не влезает» всегда ноль — реализм; число поставленного в план — «Заметки»; хвост пуст — обе проверки хвоста; причина кривой оценки не показана — оценка; звезда не ставит главное — главное; галочка не отмечает — галочка и следом «Заметки», где звонок остался открытым
- Порча шаблонов — один прогон, код 1 ровно на четырёх проверках шаблонов: без сверки дублей — первое и повторное применение («добавлено 3 из 3», строка задвоилась); без показа причины — кривая оценка; «Отменить» не снимает — последняя, главным осталась «Зарядка»
- Деплой плана дня: в скрипте живого сайта — «С прошлых дней», «Добавить в план». Шаблоны — `6c3e5ed`, запушены вместе с этой записью

*Проверено на устройствах*

- Ничего

*Не проверено*

- Всё «Готово когда» Этапа 4 — пункты проверки в Плане

**Не описано в документах — на обсуждение**

- «Из «Неразобранное»» — коряво по правилу именительного. Либо так и живём, либо у раздела другое имя
- Порядок пунктов плана — только порядок добавления, перестановки нет; у шаблонов между собой — порядок заведения
- «Сделано» у пункта из хвоста пишет `doneOn` сегодняшним днём, а не днём плана. Для «план против факта» в Этапе 5 это надо решить там
- Удалённый пункт плана — надгробие; в «план против факта» он пропадёт, будто не намечался. `dropped` не используется (Р-38) — вопрос Этапа 5
- Окно дня 8–24 — константа (Р-21), и реализм наследует её основание

Переходят из прошлых записей: напоминание при двух устройствах; без связи точка красная; хвосты Этапа 3 — Телеграм и неделя без него.

**Следующий шаг**

Проверка на телефоне по Плану, Этап 4, «Что проверить». Неделя жизни с планом — «Готово когда». По ответу: хвосты, «Сегодня» против «Учёта» (объединять ли), затем Этап 5 «Обзор недели».

---

## 13.09.2026 — Этап 3 закрыт: «Поделиться» из Chrome работает, Телеграм отложен

**Решения**

- Новых записей в `05` нет. Этап закрыт с хвостами, как Этапы 1 и 2: неделя без Телеграма проверяется жизнью, а не сессией

**Состояние: этап закрыт**

*Проверено на устройстве* — Android, со слов

- «Поделиться» из Chrome: «работает»
- YouTube: «до этого из ютуба тестил и итак всё работало» — до экрана «Заметки»; приём с тех пор не менялся, `launch.ts` прежний
- Ярлык «Записать»: «открывается поле с Делом и там можно печатать», остальное — «всё норм». Открывается ли клавиатура без тапа по полю, отдельно не названо
- Вкладка на телефоне называется «Мысли» — своё имя устройства (Р-26), умолчание «Заметки» его не трогает. Объяснено, как вернуть

*Не проверено*

- Приём из Телеграма: путь к системному «Поделиться» не нашёлся, отложено человеком. Хвост в Плане
- Неделя без записей в Телеграм — «Готово когда», хвост в Плане

**Не описано в документах — на обсуждение**

- **Зачем «Сегодня» рядом с «Учётом».** Человек: «Сегодня» — урезанный «Учёт», смысла не видно. До Этапа 4 это правда: там только кнопки и итог дня. Смысл «Сегодня» — сводка дня по модулям: план, главное дело, реализм плана рядом с учтённым временем. Записано в План к Этапу 4: после него проверить, и если дублирует — объединять

Переходят из прошлой записи: сделанное не видно до Этапов 4 и 6; экрана замысла нет; приоритет, срок и прогресс — в `05`; напоминание при двух устройствах; красная точка без связи.

**Следующий шаг**

Этап 4 «План дня» новой сессией — промпт в `06-Процесс`, «Передача в следующую сессию».

---

## 13.09.2026 — Этап 3: «Заметки» написаны целиком, ждут проверки на телефоне

**Решения — все записаны в момент выбора**

- **Р-31 — замысел — третий вид записи; дело относится к нему ссылкой, иерархии нет.** Человек: «Планы» — глобальные замыслы вроде «съездить на Байкал», дела бывают их частью. Одна ступень: ссылка `note:<id>` в `refs`. Свои группы записей отложены — миграция и поведение группы ради того, что три вида уже закрывают
- **Р-32 — вкладка входящих по умолчанию — «Заметки».** Человек предложил «Мысленное», я возразил — прячет дела, — человек согласился на «Заметки»

**Сделано**

- Логика заметок чистыми функциями с тестами: захват с видом, порядок по дню записи, группы по месяцам, поиск по словам и датам, возраст, правки карточки, связь дела с замыслом (`d7a513f`)
- Экран «Заметки»: захват с видом «Дело | Мысль | Замысел», после записи — снова дело; блок «Замыслы» со счётом дел; «Неразобранное» по месяцам с возрастом; поиск; чипы «Все · Дела · Мысли» с «Показано N из M»; карточка — ссылки из текста, вид, текст, замысел у дела, дела у замысла, «Сделано» с «Отменить», «Удалить» (`3b1e2cc`)
- Ярлык «Записать» ставит курсор в поле: `?go=inbox` ведёт на `/inbox?write=1`, экран снимает параметр; адрес ярлыка прежний (`1c9c746`)
- Импорт заметок: раздел `notes` — `text`, `kind`, `date`; строка в реестре, промпт собирается сам (`c4e8ce7`)
- «Что нового» — запись 8. Архитектура: `NoteKind` с `goal`, формат `refs`, раздел `notes` импорта

**Находки по ходу**

- Ссылка внутри строки-кнопки не нажималась бы, а ради ссылок «Поделиться» и заведён — ссылки из текста вынесены в карточку списком
- Сторож названий вкладок ловит и прежнее «Входящие», как раньше «Время»
- Прогон: шаг сценария, начатый со скобки `[`, склеился с последней строкой помощников — `SyntaxError`, смена вида не случилась. Шаг начинается с `const`
- Прогон: импорт заметок перед сценарием синхронизации запускал автопроход через пять секунд, и тот опережал кнопку «Синхронизировать», которую сценарий проверяет. Коммитов при этом ровно два — приложение право, гонка в сценарии. `SMOKE_DEBUG=1` показал это за один прогон; после импорта — пауза
- Порча нашла слабую проверку: слова «только месяц» стояли и в тексте самой записи. Проверка — по причине целиком

**Ошибки по ходу, мои**

- Проверку «только месяц» написал по словам, которые есть в данных сценария. Ловится одним: проверка прогона не ищет то, что сама же положила в данные
- Отдал решение о замысле человеку одной строкой в конце ответа и сразу начал код. Он дал «приступай», но развилка с правкой модели заслуживала явного «да» — записал её Р-31 до кода и назвал отменяемой

**Состояние: код этапа написан, этап не закрыт**

*Проверено машинно*

- `tsc` — 0; `vitest` — 458 тестов в 29 файлах; прогон — 89 проверок, ошибок нет
- Порча — каждый новый кусок сценария падал с кодом 1 ровно на своих проверках: вид после записи не возвращается на «Дело»; поиск фразой вместо слов — обе проверки поиска; смена вида в карточке; фокус по ярлыку; порядок по одному `id` — порядок загруженного; снятая проверка «только месяц»
- `npm run build` — 0. Деплой: запуск 34775310559 на `dabd4f4` успешен; в скрипте живого сайта — «Неразобранное», «Замыслы», `write`

*Проверено на устройствах*

- Ничего

*Не проверено*

- Приём «Поделиться» из Chrome, YouTube и Телеграма по отдельности — хвост Этапа 0
- Ярлык «Записать» на Android: открывается ли клавиатура сама. Если нет — захват в три действия, а не в два
- «Готово когда» Этапа 3: неделя без единой записи в Телеграм

**Не описано в документах — на обсуждение**

- Сделанное и достигнутое не видно нигде до плана дня (Этап 4) и ленты (Этап 6); «Отменить» — только сразу после
- У замысла нет своего экрана: дела видны в его карточке списком. Если дел у замыслов станет много — экран замысла
- Приоритет, срок с напоминанием, прогресс пункта плана — открытые вопросы в `05`, к Этапу 4

Переходят из прошлых записей: напоминание может врать при двух устройствах; без связи точка красная.

**Следующий шаг**

Проверка на телефоне по «Не проверено»: «Поделиться» из трёх приложений, ярлык «Записать». Неделя жизни с «Заметками» — «Готово когда». Затем Этап 4 «План дня».

---

## 13.09.2026 — Этап 3: план работ согласован, до кода — ответы на вопросы

**Решения**

- **Р-30 — карточка записи во входящих: вид, текст, «Сделано», «Удалить».** Расширение Плана, объявлено до кода, человек согласился
- «Мысль» и «наблюдение» — одно и то же, со слов человека. На экране — «Мысль»

**Сделано**

- Кода нет. План работ Этапа 3 записан в План, п. 1–5: фокус по ярлыку, карточка, порядок по `capturedOn`, поиск с датой, чипы видов, возраст, раздел импорта заметок

**Находки по ходу**

- Список входящих сортируется по `id`. Импортированная старая заметка встала бы выше сегодняшних: её ULID — момент импорта. Исправляется в п. 4
- `core/feed.ts` «Дневников» с поиском по словам не копируется: он тянет виды ленты Этапа 6 (Р-23). Правило то же, код — в модуле заметок

**Состояние: план согласован, код не начат**

**Не описано в документах — на обсуждение**

Поднял человек, записаны в `05`, «Открытые вопросы», с моими рекомендациями: название экрана входящих по умолчанию; «Планы» третьей группой; свои группы записей; приоритет — против Р-07; срок и напоминание о нём; прогресс пункта плана.

Переходят из прошлых записей: напоминание может врать при двух устройствах; без связи точка красная.

**Следующий шаг**

Ответы на вопросы Этапа 3 в `05`, затем код по Плану, порция 1 — логика входящих.

---

## 13.09.2026 — Этап 2 закрыт: репозиторий данных заведён с нуля, телефон синхронизирован

**Решения**

- Новых записей в `05` нет. README для репозитория данных — человек отдал решение разработке: вместе с гайдом установки в Этапе 7, записано в План (`e94f33f`). Он нужен тому, кто приложения не знает, а приложение читает только свои файлы — положить можно в любой момент

**Сделано**

- Репозиторий данных `DeluVremyaData` заведён приложением с компьютера с нуля: первый коммит «заведение репозитория данных» — `meta.json` через Contents API, дальше обычные коммиты синхронизации. Это и была живая проверка GitHub, которой прогон на подставном репозитории заменить не может
- Телефон подключён своим токеном
- Совет человеку по файлу-копии: вне папки проекта и не только на этом компьютере — например, папка на Google Диске; раз в месяц и перед обновлениями схемы, ежедневную копию теперь держит репозиторий данных

**Находки по ходу**

- Папки `notes/` в репозитории нет: файлы месяцев заводятся только под записи, а во входящих компьютера пусто. Появится с первой записью

**Состояние: этап закрыт**

*Проверено на устройствах*

- Компьютер, скриншот репозитория: `meta.json`, `categories.json`, `presets.json`, `reviews.json`, `templates.json`, папка `time`; три коммита — заведение, «обновлено файлов 5», «обновлено файлов 3». Со слов: «Всё работает»
- Телефон, со слов: «Тф синхронизирован»

*Не проверено*

- Хвосты Этапа 2 в Плане: режим полёта и очередь, отправка с двух устройств разом, февраль на телефоне по числу блоков

**Не описано в документах — на обсуждение**

Переходят из прошлой записи, не разбирались:

- **Напоминание может врать при двух устройствах.** Фоновая проверка (Р-24) смотрит только в свою базу и не синхронизирует
- **Без связи точка красная.** Для телефона серая — «ждёт отправки» — была бы честнее

**Следующий шаг**

Этап 3 «Входящие» новой сессией — промпт в `06-Процесс`, «Передача в следующую сессию».

---

## 13.09.2026 — Этап 2: синхронизация написана и подключена, ждёт проверки на устройствах

**Решения — все записаны в момент выбора**

- **Р-28 — записи режутся по месяцам, а не по годам.** Настоящий февраль в виде файла раскладки: 173 блока, 33 КБ. Годовой файл к декабрю — около 440 КБ на каждое нажатие, месячный — около 35 КБ. Заметки — тем же правилом
- **Р-29 — одноимённые категории сливаются сами; надгробие помнит, куда слито.** Остаётся id, совпадающий с названием, содержимое — от поздней правки. Новое поле `Category.movedTo`, версия схемы прежняя. Человек просил приоритет поздней записи: он в содержимом, а не в выборе, какая остаётся, — иначе два устройства выбрали бы разное

Отозвано до записи: папка `deluvremya/` в корне репозитория данных. Переезд в общий с «Дневниками» репозиторий — одна константа и настройка на каждом устройстве: файлы приложение собирает из своей базы, руками ничего не переносится.

**Сделано**

- Раскладка по месяцам в `core/layout.ts`: `time/ГГГГ-ММ.json`, `notes/ГГГГ-ММ.json`, без даты — `undated`, как прежде
- `ui/SyncSettings.tsx` с `a913dcb` байт в байт, отдельным коммитом (`9ce8aee`); правки — следующим
- Подключение: раздел «Синхронизация» первым в «Настройках», автозапуск из `app.tsx`, точка на шестерёнке «Сегодня»
- Р-29: `reconcilePlan` в `modules/time/categories.ts`, запуск — `watchCategoryMerges` из `app.tsx`, один на приложение, после прихода данных не своей рукой. Удаление с переносом (Р-22) пишет `movedTo`
- Прогон: подставной GitHub через CDP `Fetch` — запросы к `api.github.com` обслуживает репозиторий в памяти прогона: 409 у пустого, 422 на сдвиг не с головы, 401 на чужой токен, настоящий git blob sha. Сценарий: включить, завести пустой репозиторий, раскладка, тихий повтор, коммит другого устройства с одноимённой категорией, отказ токена, обрыв связи и очередь
- «Что нового» — запись 7
- Устаревший «ЧЕРНОВИК… до сверки не выпускается» в `starter.ts` убран

**Находки по ходу**

- **Ошибка в ядре «Дневников».** В `syncNow` ветка «не настроена» выходила раньше `try/finally` и не отпускала `running`. Проход на старте при выключенной синхронизации занимал его навсегда: включённая потом синхронизация отвечала «Не заполнены репозиторий или токен» до перезапуска приложения. Поймал новый прогон; исправлено одной строкой (`b49f37f`), тест на fake-indexeddb без исправления падает. В «Дневниках» так же — сказано человеку, их код не трогался. Возможно, их «первый запуск на телефоне провалился», списанный на гонку записи настроек, — это она
- Точка у «Дневников» горит и при выключенной синхронизации: неотправленное есть всегда. Здесь выключенная точки не даёт — `ui/syncDot.ts`, с тестом
- Без связи точка красная: «Нет связи с GitHub» — ошибка прохода, так устроено ядро. Строка «Что нового» сначала обещала «красная — сломалась»; переписана на «не прошла: нет связи, токен не принят или другое»
- Главный случай дублей категорий закрыт ещё в Этапе 1: id из названия. Остаётся встречное переименование — его и закрывает Р-29
- Импорт февраля на телефоне до обмена задвоил бы блоки: у них случайные id. Записано в План

**Ошибки по ходу, мои**

- В плане работ предупредил о дублях категорий, не открыв `categoryIdFor`, — а id из названия там с Этапа 1. Ловится одним: предупреждение о данных — после чтения кода, который их пишет
- Первый прогон проверок писал вывод в `$TMPDIR`, а в этой оболочке он пуст — три кода 1 не от кода. Вывод — в папку сессии

**Состояние: код этапа написан, этап не закрыт**

*Проверено машинно*

- `tsc` — 0; `vitest` — 418 тестов в 27 файлах; сборка — 0; прогон — 76 проверок, ошибок нет
- Порча: без строки исправления `syncNow` новый тест падает, а прогон до исправления не сошёлся на десяти проверках синхронизации. Новый сценарий прогона проверен тремя порчами, каждая — код 1 на своих проверках: без запуска слияния — две «Бег» и блок у надгробия; год вместо месяца — `time/2026.json` и пустой февраль; без красной точки — проверка отказа токена
- Ядро синхронизации расходится с `a913dcb` ровно на название приложения, строки и нарезку раскладки и исправление `syncNow`
- Деплой: запуск 34769587072 на `8dc9310` — «Сборка» и «Публикация» успешны; в скрипте живого сайта есть экран синхронизации и `movedTo`

*Проверено на устройствах*

- Ничего

*Не проверено*

- Живой GitHub. Первая синхронизация с компьютера — она же живая проверка: подставной репозиторий проверяет догадку о GitHub, а не его самого
- Всё «Готово когда» Этапа 2

**Не описано в документах — на обсуждение**

- **Напоминание может врать при двух устройствах.** Фоновая проверка (Р-24) смотрит только в свою базу и не синхронизирует: день, заполненный на компьютере, телефон считает пустым, пока приложение на нём не откроют
- **Без связи точка красная.** Для телефона, который часто без сети, серая — «ждёт отправки» — была бы честнее. Различать по коду ошибки — правка ядра или состояния синхронизации
- **README для репозитория данных.** У «Дневников» он положен руками и объясняет, что файлы не правят. Свой — сейчас или вместе с гайдом установки в Этапе 7

**Следующий шаг**

Проверка на устройствах, по порядку:

1. Компьютер: «Сохранить в файл» — страховка. Пустой приватный репозиторий через сайт GitHub, fine-grained токен только на него, «Contents: Read and write». «Настройки» → «Синхронизация»: репозиторий, токен, «Проверить доступ», «Синхронизировать». В репозитории — `meta.json`, справочники, `time/2026-02.json` и файлы текущего месяца, два коммита
2. Телефон: свой токен, тот же репозиторий. «О приложении» → «Блоки времени» — февраль целиком плюс своё; февраль не импортировать
3. Блок с телефона — на компьютере, и обратно
4. Режим полёта: блок, точка горит; сеть вернулась — очередь ушла
5. Нажатия на двух устройствах почти разом — ничего не потерялось

---

## 13.09.2026 — после закрытия Этапа 1: февраль загружен, день выбирается календарём

**Решения — все записаны в момент выбора**

- **Р-27 — день на экране учёта выбирается и календарём.** Уточняет Р-25: предпосылка «дальше недели назад листают редко» не пережила импорта февраля. Поле даты браузера между стрелками

**Сделано**

- Файл импорта февраля собран скриптом по скриншотам таблицы. Личные данные: файл — в `seed/` под `.gitignore`, скрипт — вне репозитория. Параллельное — полем «фоном», комментарии — заметками блоков
- Человек загрузил его на компьютере: «загрузило». Хвост Этапа 1 «импорт февраля» закрыт
- Поле даты между «‹» и «›» на экране учёта

**Находки по ходу**

- Скриншот 08.02 прочитан неверно, фильмы 60 вместо 50; поймала сверка по строке «Сумма» и по «Итого» дня
- В таблице два дня, где «Итого с учётом примечаний» не выводится из столбцов: 02.02 вычтено два часа параллельного при 110 минутах игр, 06.02 — поправка «со скипами» на 20 минут. У нас за месяц 211 ч 50 мин против 211 ч 20 мин в таблице — ровно эти 30 минут
- Естественный ключ импорта заставил записать 03.02 и 13.02 «наоборот» — игры основной, ютуб или прогулка фоном: иначе два одинаковых блока одного дня склеились бы в один
- Февраль лежит только в браузере компьютера. На телефон — копией файлом или первой синхронизацией Этапа 2

**Ошибки по ходу, мои**

- В Р-25 отверг календарь догадкой «дальше недели назад листают редко» — первый же импорт её опроверг. Ловится одним: решения о навигации проверять на данных за месяцы, а не за день работы

**Состояние: Этап 1 закрыт, хвост импорта закрыт**

*Проверено машинно*

- Файл февраля: сумма по каждому столбцу, «основная + фоном», равна строке «Сумма» таблицы до минуты; во всех 28 днях сумма категорий равна «Итого (мин)»; разбор импорта самого приложения (`planImport`, Node 24 без сборки) — 173 блока, 3 новые категории, не разобрано 0; повторная загрузка — 0 добавлено, 173 уже есть
- `vitest` — 395 тестов в 24 файлах; сборка — 0; прогон — 63 проверки, ошибок нет; поле даты проверено порчей — код 1

*Проверено на устройстве*

- Компьютер, со слов: «загрузило»
- После выкатки календаря, со слов: «всё работает»

*Не проверено*

- Хвосты Этапа 1 в Плане: приход напоминания самого по себе

**Не описано в документах — на обсуждение**

- Смотреть историю можно только по дням. Общая лента — Этап 6, сводка за период отложена человеком. Если по дням неудобно — поднять ленту раньше

**Следующий шаг**

Этап 2 «Синхронизация» новой сессией — промпт в `06-Процесс`. Нарезку блоков решать на числах февраля.

---

## 13.09.2026 — Этап 1 закрыт проверкой на телефоне; импорт февраля — хвостом

**Решения — все записаны в момент выбора**

- **Р-26 — названия вкладок — настройка устройства; экран учёта по умолчанию «Учёт».** «Время» вводило в заблуждение. Я предлагал одно постоянное имя — человек выбрал настройку. Тексты берут имя из `ui/screenNames.ts`, в именительном и в кавычках: своё имя не склоняется

Ответы человека на «не описано» прошлой записи: сводка за период — отложена до обзора недели; заметка блока — должна правиться; архив — объяснён, удаление добавлено прямо в архив. Фоновое время в итогах категорий — открытый вопрос в `05`, решается в Этапе 5.

**Сделано**

- Экран учёта — «Учёт»; названия трёх вкладок меняются в «Настройках» → «Названия экранов», читаются до первого экрана. Тест-сторож по всем исходникам: название вкладки, вписанное руками, валит тест
- Заметка у блока — в форме «задним числом» и в правке, видна в списке дня; убранная в форме уходит из блока
- Удаление архивной категории — прямо из архива; логика удаления вынесена в один компонент для рабочей и архивной
- Этап 1 в Плане переведён в хвосты

**Находки по ходу**

- Шаблонная строка решает подстановку имени, но не склонение: «на «Учёте»» из своего имени не собрать. Тексты переписаны на именительный в кавычках
- Нашёлся ещё один текст с именем экрана — в уведомлении напоминания: `notify.ts` читает название из настроек и в service worker

**Ошибки по ходу, мои**

- В стороже названий исключение для `screenNames.ts` записал путём от `src`, а `import.meta.glob` отдаёт пути от файла теста — сторож упал на самом источнике имён. Поправлен путь; сторож заодно показал, что больше нигде имён руками нет
- Правка «Что нового» не легла: считал строку импорта последней в записи, а за ней стояли дописанные позже. Перечитал файл

**Состояние: этап закрыт**

*Проверено машинно*

- `tsc` — 0; `vitest` — 395 тестов в 24 файлах, все прошли; сборка — 0; прогон — 63 проверки, ошибок нет
- Новые куски сценария проверены порчей, прогон падал с кодом 1 ровно на своих проверках: оповещение вкладок о новом названии, сохранение заметки, удаление из архива

*Проверено на устройстве*

- Android, со слов: «В целом всё робит», «напоминалка работает» — по сборке с кодом Этапа 1 до правок этой записи

*Не проверено*

- Импорт настоящего февраля, приход напоминания самого по себе, правки этой записи на телефоне — хвосты Этапа 1 в Плане

**Не описано в документах — на обсуждение**

- Удаление с переносом необратимо иначе как руками: блоки переписаны. Подтверждения у переноса нет — есть явный выбор категории и отдельная кнопка

**Следующий шаг**

Этап 2 «Синхронизация» новой сессией — промпт в `06-Процесс`, «Передача в следующую сессию». До первого файла в репозитории данных — импорт настоящего февраля и решение о нарезке блоков.

---

## 13.09.2026 — Этап 1: учёт времени написан целиком, ждёт проверки на телефоне

**Решения — все записаны в момент выбора**

- **Р-18 — идущий таймер — ключ `timer` в настройках устройства.** Блок без минут сломал бы суммы, импорт и слияние; цена — таймер телефона не виден на компьютере
- **Р-19 — граница дня — календарная полночь; блок таймера — днём начала.** «Логическая полночь» протекла бы в каждое «сегодня»; случай закрывает кнопка «вчера»
- **Р-20 — тап по кнопке добавляет блок, отмена — кнопкой.** Отменяет «повторный тап снимает» из Плана: с ним час чтения кнопками недостижим, а двойной тап молча обнулял запись
- **Р-21 — «неучтено» — от прошедшей части окна дня 8–24**, а не от суток
- **Р-22 — категория удаляется: пустая — сразу, с блоками — только переносом.** Перенос заодно и слияние. По просьбе человека, как в «Дневниках»
- **Р-23 — реестр растёт по этапам: сейчас подпись и импорт.** Отступление от Архитектуры, объявлено до кода; лента и markdown — Этап 6
- **Р-24 — незаполненный день — за сегодня ни одного блока.** Имя фоновой проверки `remind`, общее на все напоминания
- **Р-25 — прошлый день на «Времени» — в адресе `?day=`, кнопки пишут в показанный день.** Человек отдал решение разработке

**Сделано**

- Первым коммитом — уборка чужих стилей «Дневников» (Р-17); прогон прошёл без правок сценария
- Дата «Сегодня» сменяется в полночь без перезапуска — хук `ui/useToday`, таймер к полуночи и пересчёт при возврате из фона
- Категории и кнопки: стартовый набор с id из названия и неподвижным штампом, заводится слиянием при пустом хранилище — два устройства дублей не заведут. Порядок, переименование, признак (нигде не красит, Р-05), архив, удаление с переносом. Экран `/time/categories`
- Вкладка «Время», экран `/time` — цель ярлыка `?go=time`. Кнопки категорий и на «Сегодня»; итог дня сразу под ними — сумма с числом блоков, разбивка, фоновая отдельно, неучтённое с основанием
- Таймер и «задним числом» — одна форма на ввод и правку блока, минуты по медиане своей истории категории; «Фоном» — в форме и в таймере
- Листание дней «‹ ›» на «Времени»
- Импорт: `core/importing.ts` и экран импорта с `a913dcb` байт в байт отдельным коммитом (`7f5dcf2`), правки — следующим; `src/registry.ts`, раздел `time`, промпт из тех же описаний, сводка до записи. «Настройки» → «Экспорт и импорт» → «Импорт записей»
- Напоминание о незаполненном дне: `src/notify.ts` по механике «Дневников», обработчик `periodicsync` в `sw.ts`, раздел «Напоминания» в «Настройках»
- «Что нового» — записи 3 и 4, по строке на видимое изменение

**Находки по ходу**

- Нативная проверка формы: у поля минут стоит `min`, и браузер сам не пускал ноль своей подсказкой на своём языке — наша причина с пределами не показывалась. Формы с числами — `noValidate`. Поймал прогон
- Слово «таймер» есть в подписи «задним числом», а проверка прогона не различает регистр: «таймера нет» проверялось бы ложно. Проверка — по заголовку блока, а не по тексту
- Реестр «Дневников» связан с лентой и markdown через `core/feed.ts` — копировать его ради импорта значило тащить Этап 6 → Р-23
- Естественный ключ импорта склеивает два одинаковых блока одного дня. Для таблицы с суммами по ячейкам это верно; граница записана в Архитектуру
- `window.confirm` в безголовом браузере некому нажать — в сценарии подменяется
- Удаление категории у «Дневников» — не отдельная функция в модуле циклов, а связка экрана и плана записи; повторена механика, не код

**Ошибки по ходу, мои**

- В тесте сдвига категорий ждал изменённые записи в обратном порядке. Порядок выдачи — не требование; поправлен тест, код верен
- Вставив в прогон блок категорий, не учёл, что шаг настроек начинался с «Сегодня», где живёт шестерёнка, — восемь проверок упали не по делу. Поправлено переходом; ловится тем, что прогон запускается на каждой порции
- `min` у числового поля без `noValidate` — недосмотр; поймал сценарий, написанный на это же правило

**Состояние: код этапа написан, этап не закрыт**

*Проверено машинно*

- `tsc` — 0; `vitest` — 336 тестов в 23 файлах, все прошли; сборка — 0; прогон — 58 проверок, ошибок нет; в собранном `sw.js` нет `import`/`export`
- Каждый новый кусок сценария проверен порчей кода, прогон падал с кодом 1 ровно на своих проверках: сдвиг категорий, сумма дня, медиана, фоновая в сумме, перенос фоновой при удалении, правило незаполненного дня, разбор дня из адреса, пропуск повторов при импорте
- Копия импорта из «Дневников» — хеши блобов совпадают с `a913dcb`

*Проверено на устройстве*

- Ничего нового. Шаги 0–5 выкатывались по ходу, на телефоне их не смотрели

*Не проверено*

- Всё «Готово когда» Этапа 1 — пункты проверки в Плане, Этап 1
- Импорт настоящего февраля — человек проверяет вручную; машина проверена на тестовых данных
- Приход напоминания самого по себе — хвост Этапа 1 в Плане

**Не описано в документах — на обсуждение**

- **Сводка за месяц по категориям.** Условие этапа — «суммы февраля сходятся с таблицей», а смотреть их можно только по дням. Либо сводка периода на «Времени» сейчас, либо сверка тестом на локальной копии данных (как `realdata.test.ts` в «Дневниках»), либо ждать обзора недели
- **Заметка блока** (`note`) импортируется и хранится, но на экране не видна и не правится
- **Архивную категорию удалить нельзя**, не вернув её из архива сначала
- **Удаление с переносом необратимо** иначе как руками: блоки переписаны. Подтверждения у переноса нет — есть явный выбор категории и отдельная кнопка
- **Нарезка блоков по году или месяцу** — по-прежнему до Этапа 2, на числах настоящего февраля после импорта

**Следующий шаг**

Проверка Этапа 1 на телефоне по пунктам Плана и ручной импорт февраля. Этап 1 закрывается ответом человека; затем — Этап 2, до первого файла в репозитории данных решить нарезку блоков.

---

## 13.09.2026 — Этап 0: каркас закрыт — ставится, работает без сети, обновляется сам

**Решения — все записаны в момент выбора**

- **Р-13 — вид записи по умолчанию — дело.** Дело, записанное мыслью, сгниёт молча; мысль, записанная делом, всплывёт в разборе и переключится тапом
- **Р-14 — напоминания вместе с тем, о чём напоминать.** О незаполненном дне — последний пункт Этапа 1, об обзоре — Этап 5; механика «Дневников»
- **Р-15 — ядро целиком в Этапе 0.** `db`, `id`, `dates`, `github`, `layout`, `sync` с тестами; синхронизация подключается в Этапе 2
- **Р-16 — «Поделиться» через адрес, без service worker; картинки отложены.** `share_target` — GET на корень, разбор в `src/launch.ts`; ярлыки — `?go=`
- **Р-17 — чужие стили «Дневников» удаляются разом**, первым коммитом Этапа 1

**Сделано**

- Документы из планирования закоммичены как есть, до кода. Заведены `04-Журнал` и `06-Процесс` — на них ссылался `CLAUDE.md`, а файлов не было
- Ядро «Дневников» с их коммита `a913dcb` — байт в байт через `git archive`, отдельным коммитом `b21ecac`. Следующий, `5b5dd76`, — все правки по делу проекта: модель дословно из Архитектуры, база `deluvremya`, хранилища и индексы, раскладка, тексты, `sw.ts` без напоминаний, манифест с `share_target` и тремя ярлыками, иконка-часы
- Экраны: «Сегодня» с приветствием первого запуска; «Настройки» — «О приложении» и «Копия всех данных»; «Входящие» — минимальный захват и список
- `src/launch.ts` — адрес запуска в маршрут до первого экрана; из его же таблицы `vite.config.ts` собирает ярлыки и параметры share
- Прогон: обвязка из «Дневников», сценарий Этапа 0
- `src/changes.ts` — список «Что нового» с первого дня, окно — Этап 6
- Акцент интерфейса тёплый, в тон иконке: два приложения на одном телефоне не путаются

**Находки по ходу**

- Оба приложения на одном origin `goigolden88.github.io`. IndexedDB различается только именем — буквальная копия `DB_NAME = 'dnevniki'` открыла бы базу «Дневников». Кеши работника разведены сами: `cleanupOutdatedCaches` удаляет только кеши своего scope — проверено по коду workbox
- Приём «Поделиться» в service worker означал бы `POST`, а `POST` при неуправляющем работнике уходит на GitHub Pages, получает 405 и теряет расшаренное молча → Р-16
- Картинки через «Поделиться» обещаны в `01` и Архитектуре, а поля для них в модели нет → отложены
- Номера Р-NN в комментариях скопированного ядра — номера «Дневников». Без пометки «Р-07» в `db.ts` читалось бы как наше «приоритетов нет»; записано в Архитектуре
- Грубая оценка: годовой файл блоков времени к декабрю — около мегабайта, и синхронизация гоняла бы его целиком → открытый вопрос о нарезке по месяцам, решить до Этапа 2
- Доедет ли `#` в адресе ярлыка WebAPK — не уверен; параметр запроса доезжает наверняка, поэтому ярлыки на `?go=`
- «Поделиться» у YouTube на компьютере приложения не показывает: это собственное окно сайта YouTube, а приём share у PWA есть только на Android (Р-09). Ожидаемо, не поломка
- В системном меню «Поделиться» на телефоне приложение пока в самом конце списка; Android поднимает его по мере использования, на части телефонов его можно закрепить долгим тапом

**Ошибки по ходу, мои**

- Две правки подключения входящих сделал наполовину: импорт экрана без маршрута и таблицу ярлыков в `vite.config.ts` без импорта. Заметил, перечитав свои правки, до проверки типов; `tsc` поймал бы и так
- В сообщение коммита `974ff98` вместе с числом тестов попал хвост вывода: поиск строки «Tests» без учёта регистра зацепил и «tests 29%». Косметика; число с тех пор беру одной строкой, итог — по коду выхода

**Состояние: этап закрыт**

*Проверено машинно*

- `tsc` — 0; `vitest` — 232 теста в 13 файлах, все прошли; сборка — 0; в собранном `sw.js` нет `import`/`export`
- Прогон — 26 проверок, ошибок нет: манифест под `/DeluVremya/`, иконки отдаются, приветствие, запись во входящие, «Поделиться» с подстановкой и чисткой адреса, ярлыки, настройки, копия файлом туда и обратно, service worker, без сети — и приложение, и «Поделиться»
- Порча строки экрана входящих — прогон упал с кодом 1, восемь проверок не сошлись
- Деплой: GitHub Actions, запуск 34748785296 на `e483869` — «Сборка» (с тестами) и «Публикация» успешны; источник Pages уже стоял на GitHub Actions. На `goigolden88.github.io/DeluVremya/` страница, её скрипт и стили, иконки, манифест и `sw.js` отвечают 200; ни одной ссылки страницы мимо `/DeluVremya/`; в опубликованном манифесте `id`, `start_url`, `scope` — `/DeluVremya/`, share — GET, ярлыков три
- Сборка для проверки автообновления — запуск 34749153048 на `59b1980`, успешен; живой сайт отдаёт сборку 13.09.2026 14:14:13

*Проверено на устройстве*

- Android, со слов: «Проверил, всё робит — дата сборки 13.09.2026 14:03:31» — установка иконкой и проверка по списку: без сети, «Поделиться», ярлыки
- Автообновление, со слов: «авто обновление прошло — время поменялось на то, что ты указал» — сборка 14:03:31 сменилась на 14:14:13 сама, без переустановки
- «Поделиться» с телефона: «появилась, только надо мотать в самый конец» — приложение есть в системном меню, пока в хвосте списка

*Не проверено*

- Приём из разных приложений по отдельности — Chrome, YouTube, Телеграм кладут текст в разные параметры. Записано в хвосты Этапа 0 в Плане, проверяется в Этапе 3
- iPhone — не проверялся вовсе. Хвосты Этапа 0

**Не описано в документах — на обсуждение**

- Когда считается «первый релиз», после которого `V1_STORES` заморожен. Я считаю им установку на телефон — она случилась 13.09.2026, значит раскладка версии 1 уже заморожена
- Дата в шапке «Сегодня» не пересчитывается, если приложение открыто через полночь, — известная грабля «Дневников». Становится важной в Этапе 1, где блоки времени ставятся «сегодняшним днём»; записано в передачу

**Следующий шаг**

Этап 1 «Учёт времени» по `03-План`, новой сессией после `/clear` — промпт в `06-Процесс`, «Передача в следующую сессию». Первый коммит этапа — уборка чужих стилей (Р-17). К сессии — февральская таблица учёта досуга: её импорт — пункт 6 этапа, и на её числах решается нарезка блоков времени.
